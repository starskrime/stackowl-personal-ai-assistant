---
title: 'Nothing escapes the journal'
type: 'feature'
created: '2026-09-18'
status: 'done'
baseline_revision: '590d12a99af3c726b38be198b77c9fb196b67506'
review_loop_iteration: 0
followup_review_recommended: true
context:
  - '_bmad-output/implementation-artifacts/epic-2-context.md'
  - '_bmad-output/planning-artifacts/architecture/architecture-stackowl-personal-ai-assistant-2026-09-12/ARCHITECTURE-SPINE.md'
warnings: ['oversized']
deferred:
  - summary: >-
      The 9 record readers registered by this story (RecordReaderRegistry)
      have zero live callers outside test code -- no gateway, API, or CLI
      surface dispatches through them yet.
    evidence: |-
      Verified by search: nothing under src/stackowl/ outside
      tests/journal/test_record_readers.py calls
      RecordReaderRegistry.get()/get_record_reader_registry(). This is not a
      gap this story could close: src/stackowl/bridge/ does not exist yet
      (confirmed, no such directory anywhere in the repo), and epic 2 is
      explicitly the journal-infrastructure epic, not the Bridge-surface
      epic -- there is no consumer package to wire a caller into. AD-4/AC3's
      "runs in the gateway, md read-only" clause is correspondingly
      unimplementable today and stays unaddressed by design, not oversight.
    location: >-
      src/stackowl/journal/records.py (RecordReaderRegistry); the 9
      read_*_record functions across journal/*_events.py
    severity: low
  - summary: >-
      AD-4's separate prune-window tripwire ("owning rows referenced by
      record_ref are kept at least as long as journal retention; a tripwire
      fails any referenced record kind whose prune window is shorter") is
      not implemented and not mentioned in this story's Boundaries.
    evidence: |-
      Verified: no such tripwire exists in the diff. Not a new gap --
      deferred-work.md's pre-existing DW-17 entry (Story 2.6, predating this
      story) already assigns exactly this reconciliation to Story 2.11
      ("raises PROVISIONAL_JOURNAL_RETENTION_DAYS... and, in the SAME
      change, reconciles every subsystem prune window this tripwire
      checks"), and no unified journal-retention/prune job exists anywhere
      yet for such a tripwire to meaningfully check against -- that job is
      Story 2.11's own explicit charter (epics.md Story 2.11 ACs). No new
      deferred-work.md entry needed; DW-17 already covers it.
    location: 'ARCHITECTURE-SPINE.md AD-4; deferred-work.md DW-17'
    severity: low
---

<intent-contract>

## Intent

**Problem:** The journal (Stories 2.1-2.9) has no proof that it covers everything: `EventTypeSpec` (`journal/registry.py`) declares no table, so nothing can diff "every migration-created table" against "what the registry covers" (AD-3); event types can never gain a second `schema_version` safely because no upcaster mechanism exists at all (DW-16); and `record_ref` (AD-4) has zero registered readers anywhere — nothing can open a `{kind, locator}` target back into typed content, and a pruned/missing target has no defined "expired" behavior.

**Approach:** Give `EventTypeSpec` an optional `table` field (populated at each of the 9 existing `*_events.py` registration call sites), build `journal/coverage.py`'s `UNJOURNALED_TABLES` (every currently-live non-journal table, reasoned and explicit) plus a coverage tripwire that diffs the real migrated schema against registry-covered ∪ excused tables. Add a minimal versioned-model/upcaster registry to `EventRegistry` (`register_upcaster`, `versions_for`) plus a tripwire proving no version with live rows lost its model. Build `journal/records.py`'s reader registry keyed by `(RecordKind, carrier)` and wire one typed, authority-checked, never-raising reader per already-registered event domain, returning `ExpiredRecord` for a missing target.

## Boundaries & Constraints

**Always:** `table=` is added to every existing registration call for a sqlite-backed type (`tasks`/`jobs`/`heal_attempts`/`health_status_changes`/`turn_action_records`/`reflections`/`consent_decision_records`/`delivery_records`/`channel_ingress_records`); `memory.written` (md-backed) keeps `table=None`. `UNJOURNALED_TABLES` reasons are explicit and reviewable: a shared categorical reason for pre-epic-2 legacy tables (mirrors `health/store_cadence.py`'s own bucketed-`Cadence` precedent), a specific DW-25-citing reason for `audit_log`, and one for `journal_events` itself (the ledger, not a target). Readers are per-`(RecordKind, carrier)` functions supplied by their owning `*_events.py` module (mirrors `narrator.py::register_name_resolver`'s module-dict+`RLock`+duplicate-refusing-register+test-reset shape exactly), take `owner_id: str` and refuse (raise, never silently pass) a mismatch against `tenancy.DEFAULT_PRINCIPAL_ID` — this platform's real, single-owner authority model (same reasoning DW-25/DW-26 already used). A reader whose row is gone returns `ExpiredRecord`, never raises, never returns `None`. `md` readers only ever open a file for read (`StackowlHome`-relative path from the locator), never write. The coverage/upcaster tripwires get `@pytest.mark.tripwire` (`scripts/tripwires.sh`'s only selection mechanism) and each ships a "control" test proving it can genuinely fail (matches `test_retention_tripwire.py`'s established convention). The live "migrated table set" comes from actually running all migrations and querying `sqlite_master` (mirrors `tests/health/test_every_store_declares_its_cadence.py::_schema_tables`), never from hand-parsing `.sql` files.

**Never:** Do not wire the upcaster framework into `journal/fanout.py::read_since` or `narrator.py`'s real read path — DW-16's own text assigns that to whichever future story first bumps a `schema_version` past 1; this story ships only the registration API + the completeness tripwire. Do not build a `bridge/` package or a graph-carrier reader — `bridge/` does not exist yet (confirmed); ship a grep/AST tripwire (`bridge/` never reads a table generically) that vacuously passes today and fires the moment `bridge/` is added without complying, mirroring `test_EventTypeSpec_is_constructed_only_inside_journal`'s convention. Do not touch `DurableTaskStore.create()` or its 4 direct callers (`react_runner.py:131`, `executor.py:150`, `task_runner.py:178-211`, `rollover_summary_handler.py:400-404`, DW-7) — `tasks` is already registry-covered at the table level (the only granularity AD-3's coverage tripwire operates at per its literal epics.md text), so the table-diff tripwire correctly reports it covered; the real gap is call-site-level and `enqueue()`'s transaction does an additional loop-contract UPDATE these 4 direct-`create()` callers' already-running tasks never populate, so blindly switching them to `enqueue()` would misrepresent their lifecycle — update DW-7's ledger entry with this precise finding instead of patching a live task-execution hot path under this story's literal AC scope. Do not touch `BatchAuditor`/`AuditLogger`/`audit_log` (DW-25) — mark `audit_log` `unjournaled` with the stated reason instead; resolve DW-25's status accordingly. Do not build full md section-anchor extraction — the md reader returns the whole file's text plus the locator's anchor as metadata. Do not restart the live production core/gateway process. Do not edit `sprint-status.yaml`.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Coverage tripwire, real schema | freshly-migrated scratch DB | every live table in `registry.tables_covered()` ∪ `UNJOURNALED_TABLES` | fails, names any gap table |
| Coverage tripwire, injected new table | scratch DB + one extra ad-hoc `CREATE TABLE` after migrating | that table is in the computed gap | assertion names it (AC4) |
| Upcaster tripwire, real data | every registered type is v1-only | trivially satisfied — no version lacks a model | n/a |
| Upcaster tripwire, control case | throwaway registry: type registered only at v2, a v1 row simulated | `_missing_versions` reports `(type, 1)` | proves the check can fail |
| Reader opens an existing row | valid locator, matching `owner_id` | typed view model returned | n/a |
| Reader opens a pruned/missing row | locator's row absent | `ExpiredRecord` returned | never raises |
| Reader called with wrong owner | `owner_id != DEFAULT_PRINCIPAL_ID` | refused | raises loudly, never silent |
| md reader opens memory.written's target | valid `StackowlHome`-relative path | file text + anchor metadata returned | missing file → `ExpiredRecord` |
| Duplicate reader registration | same `(RecordKind, carrier)` registered twice | second call refused | raises `ValueError` |

</intent-contract>

## Code Map

- `src/stackowl/journal/registry.py` — add `table: str | None = None` to `EventTypeSpec` (after `intensity`, keeps every existing kwarg-call backward compatible); `EventRegistry.tables_covered() -> frozenset[str]`; a `VersionEntry` `NamedTuple(model: type[JournalAttrsBase], upcaster: Callable[[dict], dict] | None)`; `EventRegistry._kept_versions: dict[str, dict[int, VersionEntry]]`; `EventRegistry.register_upcaster(type_name, version, model, upcaster) -> None` (validates type registered, `version < current schema_version`, refuses a duplicate version); `EventRegistry.versions_for(type_name) -> dict[int, VersionEntry]` (current version + any kept older ones).
- `src/stackowl/journal/task_events.py`, `job_events.py`, `heal_events.py`, `health_events.py`, `turn_events.py`, `memory_events.py`, `consent_events.py`, `delivery_events.py`, `channel_events.py` — add `table=` to each existing `registry.register(EventTypeSpec(...))` call (task/job/heal/health/turn/consent/delivery/provider/channel → their real table names per the investigation census already done; `memory.written` → `table=None`, `memory.reflection_recorded` → `table="reflections"`).
- `src/stackowl/journal/coverage.py` (NEW) — `UNJOURNALED_TABLES: dict[str, str]`, every currently-live non-journal table from `health/store_cadence.py::DECLARATIONS` (~52 entries) mapped to a shared `_REASON_PRE_EPOCH` string, with `audit_log` → a DW-25-specific reason and `journal_events` → a self-referential reason. `coverage_gap(live_tables: Iterable[str]) -> frozenset[str]` = `frozenset(live_tables) - get_registry().tables_covered() - UNJOURNALED_TABLES.keys()`.
- `src/stackowl/journal/records.py` (NEW) — `ExpiredRecord` (frozen pydantic: `record_kind`, `locator`, `reason`); `RecordReader` protocol; `RecordReaderRegistry` (module dict keyed `(RecordKind, Literal["sqlite","md","graph"])`, `threading.RLock`, `register()` refuses a duplicate key, `get()`, `reset_record_readers_for_tests()` — mirrors `narrator.py::register_name_resolver`/`_resolvers` exactly); shared `_read_sqlite_record(db_pool, *, table, id_column, id_value, view_model) -> BaseModel | ExpiredRecord` and `_read_md_record(locator, view_model) -> BaseModel | ExpiredRecord` internal helpers used by every module's own typed reader.
- `task_events.py`/`job_events.py`/`heal_events.py`/`health_events.py`/`turn_events.py`/`consent_events.py`/`delivery_events.py`/`channel_events.py`/`memory_events.py` — each adds one small `*RecordView` pydantic model + one `read_*_record(db_pool, locator, *, owner_id)` function calling the shared helper with its own table/id-column (`task_id`/`job_id`/`id`/`id`/`id`/`id`/`id`(×2 for delivery+provider, same table)/`id`/`trace_id`), registered against `RecordReaderRegistry` for its `(RecordKind, "sqlite")` (or `"md"` for memory.written) key.
- `src/stackowl/db/migrations/*.sql` — read only, not modified (no new tables/columns needed).
- `tests/journal/test_coverage_tripwire.py` (NEW, `@pytest.mark.tripwire`) — `_schema_tables(db_path)` (mirrors `tests/health/test_every_store_declares_its_cadence.py`); real-schema coverage assertion; a "no table both covered and excused" sanity check; AC4's control test (inject an extra table, assert it's in the gap).
- `tests/journal/test_upcaster_tripwire.py` (NEW, `@pytest.mark.tripwire`) — pure `_missing_versions` unit tests (real registry: empty; control: throwaway `EventRegistry()` with a v2-only fake type and a simulated v1 row).
- `tests/journal/test_record_readers.py` (NEW) — one round-trip test per registered reader (`tmp_db`, insert a real row via the module's existing recording helper, read it back, assert typed fields); expired-row test per reader; wrong-`owner_id` refusal test; duplicate-registration refusal test.
- `tests/journal/test_bridge_never_reads_a_table_generically.py` (NEW, `@pytest.mark.tripwire`) — grep/AST guard over `src/stackowl/bridge/` (vacuously passes — directory doesn't exist yet).
- `_bmad-output/implementation-artifacts/deferred-work.md` — update DW-7 (stays `open`, precise table-vs-call-site finding appended) and DW-25 (→ `resolved`, cites `journal/coverage.py::UNJOURNALED_TABLES["audit_log"]`).

## Tasks & Acceptance

**Execution:** `registry.py` (`table` field + `tables_covered`/upcaster API) → the 9 `*_events.py` files' `table=` kwargs → `coverage.py` → `records.py` → the 9 `*_events.py` files' reader additions → all new tests → `deferred-work.md` updates.

**Acceptance Criteria:**
- Given the migrated table set, when the coverage tripwire runs, then every table is `registry.tables_covered()` or `UNJOURNALED_TABLES` with a written reason, diffed against the real schema (AD-3).
- Given an event type with >1 `schema_version`, when the upcaster tripwire runs, then it fails if any version with live rows (by `MIN(cursor)`) lost its model/upcaster; proven via a control case since no real type has >1 version yet.
- Given each `(RecordKind, carrier)` pair with a registered reader, when it runs, then it is typed, authority-checked (`owner_id`), and a missing target opens `ExpiredRecord`, never an error.
- Given a new migration adds an uncovered table, when the tripwires run, then the coverage tripwire fails and names it.

## Design Notes

**Why `EventTypeSpec.table` instead of a hand-maintained type→table dict:** a parallel dict can drift from the real registration call sites exactly the way `health/store_cadence.py`'s own docstring warns against ("a hand-maintained list rots the first time someone adds a table and forgets") — putting `table` on the spec itself, set at the same call site as the type's other metadata, removes that drift class entirely.

**Why `(RecordKind, carrier)` instead of `RecordKind` alone:** AD-4's "each record kind has one registered reader" reads naturally as the `RecordRef.kind` carrier (`sqlite`/`md`/`graph`), but "supplied by the owning subsystem" (task/job/heal/...) is `journal.enums.RecordKind`. `memory` is the one domain that needs both (`memory.written` is `md`, `memory.reflection_recorded` is `sqlite`) — keying by the pair resolves the ambiguity without inventing two competing registries.

**Why `owner_id == DEFAULT_PRINCIPAL_ID` is the authority check:** no generic `authz.may()` exists; the established idiom (`tenancy.OwnedRepository`) is row-level `owner_id` scoping, but none of the journal record-ref tables carry an `owner_id` column (confirmed: `turn_action_records`, etc.) — this is a single-owner platform (the same reasoning DW-25/DW-26's review threads already used), so the honest, minimal "authority check" a reader can make today is refusing any caller that isn't the one owner, loudly.

## Verification

**Commands:**
- `uv run pytest tests/journal/ -q` -- expected: all new and existing tests pass.
- `./scripts/tripwires.sh` -- expected: exits 0, ruff/mypy baselines unchanged or improved.

**Manual checks (if no CLI):**
- Left to the coordinator: none required — this story adds no new production write path and does not touch the live core/gateway process.

## Review Triage Log

- **low, patch** — `task_events.py`/`heal_events.py`/`health_events.py`/`job_events.py`/`memory_events.py` each define a module-level `_TABLE`/`_REFLECTIONS_TABLE` constant used only by the new reader function, while their `_register()` `EventTypeSpec(...)` calls still pass the table name as a duplicated string literal (`table="tasks"`, `table="heal_attempts"`, `table="health_status_changes"`, `table="jobs"`, `table="reflections"`). Verified by reading each file: the constant and the literal are two separate spellings of the same fact, reintroducing the exact drift class (`EventTypeSpec.table` set at a different point than the type's other metadata) the story's own Design Notes say putting `table` on the spec eliminates. `channel_events.py`/`consent_events.py`/`delivery_events.py`/`turn_events.py` already do it correctly (`table=_TABLE`). Fix is a direct substitution, no new surface.
- **low, patch** — `deferred-work.md` DW-7's `location:` field (line 61) still cites `rollover_summary_handler.py:399`, but the newly appended `reason:` finding text cites `rollover_summary_handler.py:400-404` for the same call site. Verified by reading `rollover_summary_handler.py:395-404`: the real `store.create(DurableTask(...))` call spans lines 400-404 (line 399 is `now = datetime.now(UTC)`), so the appended text is correct and the `location:` field is the stale one. Direct one-line correction.
- **medium, patch** — `tests/journal/test_bridge_never_reads_a_table_generically.py`'s guard regex (`(SELECT|INSERT|UPDATE|DELETE)\b[^\n]*\{[^\n}]*(locator|record_ref)`) only matches when the SQL verb and the `{locator|record_ref}` interpolation appear on the same source line. Verified: an ordinary two-line refactor (`table = locator["table"]` on one line, `f"SELECT * FROM {table}"` on the next) produces a real generic-table read but never matches the pattern, defeating the entire guard's purpose while it keeps reporting a clean pass. `bridge/` doesn't exist yet so there is no current false-negative in production, but the tripwire's whole job is to catch this shape the moment `bridge/` appears, and it currently would not. Widen the scan to be a function/module-scope check (e.g. flag any function whose body contains both a SQL-verb string and a `locator`/`record_ref`-sourced variable, not requiring same-line co-occurrence) rather than a single-line regex; add a control case proving the widened scan still catches the two-line shape.
- **medium, patch** — `journal/task_events.py::read_task_record` queries `SELECT * FROM tasks WHERE task_id = ?` with no `owner_id` filter. Verified: `tasks`' real schema (`db/migrations/0045_durable_tasks.sql:26-37`) has `PRIMARY KEY (owner_id, task_id)`, not `task_id` alone, and `DurableTaskStore.__init__` accepts an arbitrary `owner_id` (used with non-default values in `tools/agents/delegate_task.py`, `scheduler/handlers/task_liveness_sweep.py`, etc., all currently resolving to `DEFAULT_PRINCIPAL_ID` only by a documented-but-not-enforced "single-user invariant" comment, not a database constraint). The reader's `refuse_unless_owner` check already establishes the caller IS the platform owner before reading — the read itself should also scope `WHERE task_id = ? AND owner_id = ?` using that same owner_id, matching the row-level `OwnedRepository` scoping idiom DW-25/DW-26 already established as this platform's authority model (the spec's own Design Notes claim "none of the journal record-ref tables carry an owner_id column," which is true for `turn_action_records` etc. but false for `tasks` specifically). Not reachable today given the single-owner invariant holding everywhere else in the codebase, but the fix is trivial and self-contained to `read_task_record` alone (add the `owner_id` predicate to its own query; no change to the shared `read_sqlite_record` helper's contract for the other 8 readers required).
- **low, patch** — No test directly asserts `stackowl.journal.records._DEFAULT_PRINCIPAL_ID == stackowl.tenancy.DEFAULT_PRINCIPAL_ID`. Verified the values currently match (`tenancy/principal.py`) and that a real drift would already fail every round-trip test indirectly, so this is a diagnostics improvement, not a live bug. Fix is one trivial assertion test.
- **low, patch** — `journal/coverage.py::UNJOURNALED_TABLES` lists `delivery_attempts` (pre-Epic-2 legacy) immediately next to the registry-covered `delivery_records` (Epic 2, `delivery_events.py`) with no comment distinguishing the two near-identically-named tables. Verified both table names are real and distinct. Fix is one clarifying inline comment.
- **low, false** — Blind Hunter and Edge Case Hunter both flagged `read_sqlite_record`/`read_md_record` for having no `try/except` around `view_model.model_validate(...)` or the `is_file()`→`read_text()` sequence, arguing an uncaught `ValidationError`/`OSError` would contradict the readers' "never raises except for owner refusal" contract. Verified: the spec's own frozen Boundaries text scopes that guarantee to "a reader whose row is gone" — not a present-but-malformed row — and every registered type today is v1-only (proven by the upcaster tripwire), so a shape mismatch between a writer and its own reader's view model cannot currently occur; the TOCTOU window is a same-process nanosecond race with no code path that deletes memory files concurrently with a read. Rejected: the bad outcome is not reachable in today's system, and the proposed fix (wrapping in guards) is more than a direct correction.
- **low, false** — Blind Hunter flagged `get_record_reader_registry().register(...)` calls as bare module-level statements (unlike `EventTypeSpec` registration's `_register()` wrapper), meaning `reset_record_readers_for_tests()` leaves the registry unrecoverable short of a re-import. Verified `reset_record_readers_for_tests()` has zero call sites anywhere in the test suite today — the described breakage requires a future test that doesn't exist. Rejected: not reachable today, and a real fix (re-callable registration wrappers across all 9 modules) is more than a direct correction.
- **low, false** — Blind Hunter flagged `tests/journal/test_record_readers.py` for hand-constructing `locator` dicts instead of reading the real `record_ref.locator` a writer emitted, risking an undetected writer/reader shape drift. Verified independently (and confirmed by the verification-gap layer, which traced all 9 real writers against their readers): every hand-built test locator matches the real emitted `record_ref.locator` shape exactly (e.g. `pipeline/durable/store.py:1270`'s `{"table": ..., "task_id": ...}` for tasks). Rejected: no current drift exists, and rewriting all 9 reader tests to fetch locators dynamically is more than a direct correction.
- **low, false** — Blind Hunter flagged `ExpiredRecord.locator: dict[str, str]` as only superficially immutable under `frozen=True` (blocks reassignment, not in-place mutation, and may share the caller's own dict object). Verified: nothing in the current codebase mutates a returned `ExpiredRecord.locator` in place. Rejected: not reachable today, and the fix (defensive copy or `MappingProxyType`) is more than a direct correction for a currently-inert risk.
- **low, false** — Blind Hunter flagged `read_sqlite_record` returning `None` identically for "no `db_pool` wired" and "row genuinely gone," producing a misleading `ExpiredRecord.reason` in the former case. Verified no production caller exists yet that would invoke a sqlite-backed reader with `db_pool=None` (the registry has no wired consumer this story; every sqlite round-trip test passes a real pool). Rejected: not reachable today, and distinguishing the two cases is more than a direct correction.
- **false** — Edge Case Hunter claimed (high confidence) the upcaster tripwire's "real data" test never queries live `journal_events` rows via `MIN(cursor)`, only a synthetic assumption seeded from the registry's own current `schema_version`. Verified against the spec's own frozen I/O & Edge-Case Matrix ("Upcaster tripwire, real data | every registered type is v1-only | trivially satisfied — no version lacks a model | n/a") and Boundaries ("Do not wire the upcaster framework into `journal/fanout.py::read_since`... this story ships only the registration API + the completeness tripwire"): the intent explicitly excludes a live `MIN(cursor)` query from this story's scope, assigning it to whichever future story first bumps a real `schema_version`. Rejected: matches spec, not a gap this story must close.

### 2026-09-18 — Review pass (bmad-build-auto step-04, independent of the implementation subagent's own internal review pass above)

This pass ran 4 fresh, context-free reviewer subagents (blind-hunter, edge-case-hunter, verification-gap, intent-alignment) against the full diff since `baseline_revision`, per the orchestrating workflow's own step-04 — independent of, and in addition to, the internal review the implementation subagent already ran and logged above. 27 findings reported; every finding independently re-verified against the actual code before a verdict was rendered (not taken on faith from any layer, including where a finding matches a claim already logged above).

**On the one entry below that revisits an already-logged row (the upcaster tripwire's "real data" check):** the row above rejected the same claim as `false`, reasoning that the spec's own Boundaries/I-O-matrix text excludes a live query from scope. Step-04's own triage rule states: "reject or defer a finding as out of scope only when the intent itself excludes it — not because the spec's scope section... says so. If only [the spec] would exclude it, keep the finding." The prior rejection's own stated reasoning cites only the spec, not the intent (epics.md AC2's literal text, which does describe a `MIN(cursor)`-checked live query) — so per this workflow's own rule, that rejection does not stand as an "out of scope" disposition. This pass overrides it accordingly, on fresh, independent triple corroboration (blind-hunter, edge-case-hunter as a `claim` finding, and intent-alignment's descriptive divergence report each reached it independently), rather than silently re-asserting the same disposition.

- verdicts: 27 findings — high 0, medium 4, low 21, false 2, maybe-false 0
- findings:
  - `[medium]` `[patch]` (blind-hunter, B9) The upcaster tripwire's "real data" test (`tests/journal/test_upcaster_tripwire.py::TestRealRegistryEveryTypeIsVOnly`) builds its own input from the registry's own current `schema_version` per type, so it can only ever pass by construction — it never queries live `journal_events` rows, unlike AC2's literal text ("checked against `MIN(cursor)` for that version"). Verified: `_missing_versions` takes a caller-supplied `live_versions_by_type` dict; nothing in the diff runs a real `SELECT type, schema_version, MIN(cursor) FROM journal_events GROUP BY type, schema_version` anywhere. Real, in-scope per the override reasoning above. Fix: add one additional integration test using the `tmp_db` fixture — record a couple of real events through an existing emitter, run the real grouped `MIN(cursor)` query, and feed the result into the existing `_missing_versions` (additive; the existing synthetic/control tests stay as-is).
  - `[medium]` `[patch]` (edge-case-hunter, E9, `claim`) Same claim, independently found via the claims-check pass against the spec's own Intent text ("a tripwire proving no version with live rows lost its model"). Grouped with the row above; same fix, same verdict.
  - `[medium]` `[patch]` (intent-alignment, I1) Same divergence, independently found via AC2 vs. diff comparison. Grouped with the two rows above; same fix, same verdict.
  - `[medium]` `[patch]` (blind-hunter, B2) `tests/journal/test_coverage_tripwire.py`'s `test_no_table_is_both_covered_and_excused` only catches a table that is both covered and excused — nothing asserts every `UNJOURNALED_TABLES` entry still exists in the live schema, so a table dropped by a future migration leaves a permanently stale excuse with no tripwire to catch it. Verified: no such assertion exists in the file. Real: `health/store_cadence.py`'s own docstring, which this module explicitly cites as its design precedent, states "BOTH DIRECTIONS MATTER" and its own test enforces both; this story's coverage tripwire only enforces one. Fix: add `test_no_excused_table_is_stale` asserting `UNJOURNALED_TABLES.keys() - live_tables == set()` against the same freshly-migrated schema the other tests use.
  - `[low]` `[patch]` (edge-case-hunter, E1) `read_sqlite_record`'s `id_value=locator.get(id_column_key, "")` pattern means a locator missing its id key silently becomes an empty-string lookup, finds no row, and reports "the row this event referenced is gone" — indistinguishable from a genuinely pruned row. Verified: no current real caller constructs an incomplete locator (all 9 writers' `record_ref.locator` shapes match their readers' expectations exactly), so this is unreachable today, but the fix is a direct one-line addition. Fix: log a DEBUG line in `read_sqlite_record` (and the `read_task_record` local-query variant) when `id_value` is falsy, noting the locator was malformed, before returning `None` — no behavior change, `ExpiredRecord` is still returned, only the operator-facing log gains the distinction.
  - `[low]` `[patch]` (edge-case-hunter, E2) Same root cause, `journal/task_events.py::read_task_record`'s local `locator.get("task_id", "")`. Grouped with the row above; same fix.
  - `[low]` `[patch]` (blind-hunter, B6) `EventRegistry.register_upcaster` validates that `version` is older than the type's current `schema_version` and that the `(type, version)` pair isn't a duplicate, but never validates that `model` is actually a `JournalAttrsBase` subclass — unlike this same file's `EventTypeSpec.__post_init__`, which validates its own cross-field invariants at construction. Verified: no `isinstance`/`issubclass` check exists. Unreachable today (`register_upcaster` has zero real call sites — no event type has bumped past v1 yet), but the fix is a direct one-line addition matching the file's own established validation convention. Fix: raise `ValueError` when `not (isinstance(model, type) and issubclass(model, JournalAttrsBase))`.
  - `[low]` `[patch]` (edge-case-hunter, E5) Same method, `register_upcaster` also never validates `version >= 1` (a nonsensical `version=0` or negative would be silently accepted). Grouped with the row above; same fix (add a second direct guard, `version < 1`).
  - `[low]` `[patch]` (blind-hunter, B1) `deferred-work.md`'s DW-16 entry ("no schema-version upcaster framework exists") is not updated even though this story ships DW-16's own foundational registration API (`register_upcaster`/`versions_for`) — unlike DW-7 and DW-25, which this story's diff already updates. Verified: DW-16's entry is untouched in the diff. Fix: append a dated note to DW-16 (same style already used for DW-7/DW-25 in this diff) stating Story 2.10 added the registry-level API and its completeness tripwire, while the real dispatch wiring into `journal/fanout.py`'s read path remains DW-16's own stated open item for whichever future story first bumps a real `schema_version`.
  - `[low]` `[patch]` (blind-hunter, B12) `journal/coverage.py::UNJOURNALED_TABLES` disambiguates `delivery_attempts` from the registry-covered `delivery_records` with an inline comment, but no equivalent scan or comment covers other near-identical pairs. Verified one concrete, higher-stakes case exists: `"turn_decisions"` (pre-Epic-2, migration 0071) vs. the registry-covered `turn_action_records` — this is DW-20's own already-documented real historical near-collision (a completely different schema, actively read by `pipeline/decision_store.py::TurnDecisionStore` for the ADR-7 `/explain` surface), and `UNJOURNALED_TABLES["turn_decisions"]` today carries only the generic shared reason with no disambiguating note. Fix: add a short comment above the `"turn_decisions"` entry, mirroring the existing `delivery_attempts` comment, citing DW-20. The broader ask this same finding also raised — a full pairwise-similarity scan across all ~61 tables — is not pursued: no other pair carries a documented historical confusion incident the way this one (DW-20) does, and a generic name-similarity scanner for a one-time table census is more than this row's direct correction.
  - `[low]` `[false]` (blind-hunter, B8 — carried) `read_sqlite_record`/`read_md_record` have no `try/except`, contradicting the readers' "never raises" contract. Same claim, same location, already logged and rejected above (2026-09-18, "low, false" — "the spec's own frozen Boundaries text scopes that guarantee to 'a reader whose row is gone'... every registered type today is v1-only... the TOCTOU window is a same-process nanosecond race with no code path that deletes memory files concurrently with a read"); the code still reads exactly as that row describes. Carried forward unchanged — independently re-verified by me and I concur with the original disposition (unlike the upcaster-tripwire entry above, this rejection's own reasoning is grounded in the spec's Boundaries text ACCURATELY reflecting the intent's own AC3 text, which is equally narrow: "a target that no longer exists opens as an expired record" — not "any read failure whatsoever").
  - `[low]` `[false]` (edge-case-hunter, E3 — carried) Same claim narrowed to `read_sqlite_record`'s `model_validate(...)` call specifically (a `ValidationError` on schema drift). Same location, same disposition as the row above; grouped, not a distinct root cause.
  - `[low]` `[false]` (edge-case-hunter, E4 — carried) Same claim narrowed to `read_md_record`'s `is_file()`→`read_text()` TOCTOU window specifically (`OSError`/`UnicodeDecodeError`). Same location class, same disposition as the two rows above; grouped, not a distinct root cause.
  - `[low]` `[reject]` (blind-hunter, B3) `journal/task_events.py::read_task_record` reimplements fetch-and-validate logic (its own `_SELECT_TASK_SQL`, its own `db_pool is None` guard, its own `model_validate` call) instead of extending the shared `read_sqlite_record` helper with an optional extra `WHERE` predicate. Verified: true, and it is a real duplication. Rejected: purely stylistic (no wrong outcome for any caller — confirmed by this table's own passing round-trip/expired/owner-refusal tests), and the fix touches a shared helper's public signature that the other 8 working, tested readers also depend on — more than a direct correction for a zero-functional-impact nit.
  - `[low]` `[false]` (blind-hunter, B4 — carried) Bare module-level `get_record_reader_registry().register(...)` calls, unlike `EventTypeSpec`'s `_register()` wrapper, leave `reset_record_readers_for_tests()` with no re-population path. Same claim, same location, already logged and rejected above ("`reset_record_readers_for_tests()` has zero call sites anywhere in the test suite today"). Carried forward unchanged — independently re-verified, still zero call sites, still not reachable.
  - `[low]` `[defer]` (blind-hunter, B5) Nothing in the diff calls `RecordReaderRegistry.get()` outside test code — no gateway, API, or CLI consumer dispatches through the 9 registered readers; the mechanism proves itself only in tests. Verified: true, confirmed by search. Not this story's problem to close: `src/stackowl/bridge/` does not exist yet (confirmed, no such directory), and epic 2 is explicitly the journal-infrastructure epic, not the Bridge-surface epic — there is no consumer package this story could wire a caller into. Deferred: log a new `deferred-work.md` entry noting the reader registry has zero live callers pending a `bridge/` (or equivalent) consumer.
  - `[low]` `[defer]` (intent-alignment, I2) Same underlying fact from the AC3-vs-diff angle: "the 'runs in the gateway, md read-only' clause of AD-4/AC3 is unimplementable until a gateway surface exists... that leaves this specific clause of AC3 wholly unaddressed." Grouped with the row above; same disposition, same new deferred-work entry.
  - `[low]` `[reject]` (edge-case-hunter, E6) `RecordReaderRegistry.register()`/`.get()` accept any string as `carrier` at runtime — `Carrier = Literal["sqlite","md","graph"]` is a type-checker-only alias, not runtime-enforced. Verified: true, no `isinstance`/membership check exists. Rejected: every one of the 9 real call sites in this diff passes a literal string constant, which `mypy` (enforced by `scripts/tripwires.sh`'s baseline gate) already rejects if misspelled — the type system fully covers every reachable call site in this story's scope; a runtime guard would defend against a dynamically-constructed carrier string that exists nowhere in the diff.
  - `[low]` `[reject]` (edge-case-hunter, E7) The `bridge/` guard's AST co-occurrence scan cannot see data flow split across functions or files (a generic table read built via a cross-function helper would pass it). Verified: true, an inherent limit of the lightweight heuristic, not a bug — the same class of limitation the codebase's own established precedent (`test_EventTypeSpec_is_constructed_only_inside_journal`) already accepts. Rejected: `bridge/` does not exist yet to demonstrate an actual false negative; building real cross-file dataflow analysis for a placeholder guard is far more than a direct correction.
  - `[low]` `[reject]` (edge-case-hunter, E8) Same guard, the opposite direction: a docstring merely mentioning both a SQL verb and `locator`/`record_ref` could theoretically false-positive. Verified: true in principle, but a false positive is a safe failure (blocks a compliant function, forces human review, lets nothing bad through) for a guard that only ever runs once `bridge/` exists. Rejected: same "more than a direct correction for a placeholder guard" reasoning as the row above; grouped by shared root cause (the scan's approximate, heuristic nature) but kept as its own row per its own distinct trigger direction.
  - `[low]` `[reject]` (edge-case-hunter, E10) `journal/coverage.py::UNJOURNALED_TABLES` includes `"journal_events"` itself despite this story's own spec Code Map bullet describing the dict as covering "every currently-live non-journal table." Verified: true, a real spec-prose imprecision (the code's behavior — excusing `journal_events` with its own specific, correct, self-referential reason — is right; only the Code Map's summary phrase is imprecise). Rejected per rule: the only fix is editing this build's spec.
  - `[low]` `[false]` (blind-hunter, B7 — carried) `ExpiredRecord.locator: dict[str, str]` is `frozen=True` but does not defend against in-place mutation of the dict object, nor defensively copy the caller's dict. Same claim, same location, already logged and rejected above ("nothing in the current codebase mutates a returned `ExpiredRecord.locator` in place"). Carried forward unchanged — independently re-verified, still true.
  - `[low]` `[reject]` (blind-hunter, B10) Nine `read_*_record` functions each hand-roll "turn `None` into a domain-specific `ExpiredRecord`" rather than factoring it into one more shared helper. Verified: true. Rejected: zero functional impact — each domain's `ExpiredRecord.reason` string is accurately specific to its own table, which is the story's own established design preference (explicit per-site declarations over a shared abstraction that would either genericize the reason or add a parameter for marginal benefit); purely stylistic.
  - `[low]` `[reject]` (blind-hunter, B11) The diff is large (~136KB across 9 production modules, 2 new modules, 4 new test files) and the implementation subagent's own internal pass set `followup_review_recommended: false`, naming a reviewability risk from landing without a second independent pass. Moot: this very step-04 pass is that second, independent review.
  - `[low]` `[defer]` (intent-alignment, I3) AD-4's rule text includes a distinct clause this story's diff does not implement or mention as excluded: "Owning rows referenced by `record_ref` are kept at least as long as journal retention; a tripwire fails any referenced record kind whose prune window is shorter." Verified: no such tripwire exists in the diff, and neither the spec's Boundaries nor its Deferred sections name it. Not a new gap this story introduces: `deferred-work.md`'s pre-existing DW-17 entry (from Story 2.6, predating this story) already assigns exactly this reconciliation — "Story 2.11 raises `PROVISIONAL_JOURNAL_RETENTION_DAYS`... and, in the SAME change, reconciles every subsystem prune window this tripwire checks... so none is shorter than the new value" — and no unified journal-retention/prune job exists anywhere yet for this tripwire to meaningfully check against (Story 2.11's own explicit charter). Deferred to Story 2.11 per the pre-existing DW-17 assignment; no new ledger entry needed (DW-17 already covers it).
  - `[false]` `[reject]` (intent-alignment, I4) DW-7's own text offered exactly two closing branches (wire `create()`'s callers through the journal, or mark the path `unjournaled`); the diff's DW-7 update does neither, instead leaving it `open` with a refined, call-site-level finding. Refuted as a defect: re-reading `pipeline/durable/store.py`'s `enqueue()` (does an additional loop-contract UPDATE these 4 direct-`create()` callers' already-running tasks never populate) confirms branch 1 would misrepresent their lifecycle, and `tasks` being already registry-covered means branch 2 ("mark unjournaled") does not fit either — both of DW-7's own originally-proposed branches are unsound once the real code is read. The diff's actual disposition is a more correct, better-reasoned outcome than either literal branch, not a gap.
  - `[false]` `[reject]` (intent-alignment, I5) "QA/review evidence lives in prose, not run output" — the diff artifact contains no captured `pytest`/`tripwires.sh` output. Refuted: a `git diff` never embeds command transcripts by its nature, for any change, in any story; this is not a property specific to this diff. The underlying house rule (verification actually performed) was independently confirmed: I re-ran `uv run pytest tests/journal/ -q` (207 passed) and `./scripts/tripwires.sh` (722 passed, 2 skipped, ruff 29/mypy 57, both within baseline) myself, directly, before this review pass.

## Auto Run Result

**Summary of implemented change:** `EventTypeSpec` (`journal/registry.py`) gained an optional `table` field, set at every existing registration call site for a sqlite-backed type, so `EventRegistry.tables_covered()` can name every table a registered event's `record_ref` writes into without a second, drift-prone type→table mapping. `journal/coverage.py` (new) declares `UNJOURNALED_TABLES` — every other currently-live table, explicitly reasoned (a shared categorical reason for ~50 pre-Epic-2 legacy tables, a DW-25-specific reason for `audit_log`, a self-referential reason for `journal_events` itself) — and `coverage_gap()` diffs the real, freshly-migrated schema against `tables_covered() ∪ UNJOURNALED_TABLES`, satisfying AC1/AC4. `EventRegistry` also gained a minimal versioned-model/upcaster API (`register_upcaster`, `versions_for`, `VersionEntry`) plus a completeness tripwire proving no version with live rows loses its registered model (AC2) — including, after the patch round, a real integration test that records genuine events and runs an actual `MIN(cursor)`-grouped query against `journal_events`, not only a synthetic proof of the mechanism. `journal/records.py` (new) builds `RecordReaderRegistry`, keyed by `(RecordKind, carrier)`, plus `ExpiredRecord` and the shared `read_sqlite_record`/`read_md_record` helpers; all 9 event domains (`task_events.py` through `memory_events.py`) each register one typed, owner-checked reader (11 registrations total — `memory` covers both its `sqlite` and `md` carriers, `delivery`/`provider` share one table with two readers), satisfying AC3. A new AST-based tripwire (`tests/journal/test_bridge_never_reads_a_table_generically.py`) guards AD-4's "`bridge/` never reads a table generically" rule ahead of that package existing.

**Files changed** (19 in the implementation pass + 8 touched again in the patch round, all already covered by the list above; not re-listed here for brevity — see the diff since `baseline_revision`).

**Review findings breakdown:** Two review passes ran. (1) The implementation subagent's own internal pass (blind-hunter, edge-case-hunter, verification-gap) — 23 findings, 6 patched (table-constant duplication, a stale DW-7 line reference, the bridge-guard's same-line-only regex, a missing `owner_id` filter on `tasks` reads, a missing `_DEFAULT_PRINCIPAL_ID` drift test, a `delivery_attempts`/`delivery_records` disambiguating comment), 17 rejected as false or as low-and-unreachable-with-a-non-trivial-fix. (2) This orchestrator's own independent step-04 pass (blind-hunter, edge-case-hunter, verification-gap, intent-alignment) — 27 findings, every one independently re-verified against the actual code (not taken on faith, including where a finding repeated a claim the first pass had already rejected):
- **Patched (4 medium, 6 low across 6 grouped entries):** the upcaster tripwire's "real data" test never queried live `journal_events` (found independently by 3 layers; the first pass's own rejection of this exact claim was overridden — its stated reasoning cited only the spec, not the intent, which epics.md AC2's literal `MIN(cursor)` text does not exclude) — a real integration test now records genuine events and runs the actual grouped query; the coverage tripwire checked only one of the two directions `health/store_cadence.py`'s own cited precedent enforces (a table dropped from the schema would leave a permanently stale `UNJOURNALED_TABLES` excuse) — `test_no_excused_table_is_stale` added; `read_sqlite_record`/`read_task_record` silently treated a locator missing its id key the same as a genuinely pruned row — a DEBUG log line now distinguishes them; `register_upcaster` validated version ordering and duplication but not that `model` is a `JournalAttrsBase` subclass nor that `version >= 1` — both guards added, matching the file's own established validation convention; DW-16 (the upcaster-framework gap this story's own Intent names as a problem it solves) was left unupdated in `deferred-work.md` despite this story shipping its foundational API — a dated note now credits the API and reaffirms the real dispatch-wiring is still DW-16's own open item; `UNJOURNALED_TABLES["turn_decisions"]` carried no disambiguating comment despite being DW-20's own documented real historical near-collision with the registry-covered `turn_action_records` — a comment was added, citing DW-20. All 6 fixes independently re-verified by me after the patch round: full targeted suite (`tests/journal/ -q`) → 211 passed (4 new), full `./scripts/tripwires.sh` → TRIPWIRES PASS, 726 passed/2 skipped (4 new tripwire tests), ruff 29/mypy 57 (baselines unchanged).
- **Deferred (2 entries, both low; both added to this spec's frontmatter `deferred:` and one — the readers-have-no-consumer finding — also logged as `deferred-work.md` DW-29):** the 9 record readers this story registers have zero live callers (no `bridge/` package exists yet to dispatch through them — confirmed, and out of epic 2's own charter to build); AD-4's separate prune-window tripwire is absent, but that reconciliation is already, explicitly assigned to Story 2.11 by the pre-existing DW-17 entry (Story 2.6), so no new ledger entry was needed for it.
- **Rejected (15 entries — 2 false, 13 low):** an uncaught-exception TOCTOU/schema-drift claim against `read_sqlite_record`/`read_md_record` (carried forward from the first pass's own rejection, re-verified and concurred with — the "never raises" guarantee is scoped to "row is gone" by both the spec's Boundaries and the intent's own AC3 text, not to malformed data); `read_task_record`'s fetch-logic duplication vs. the shared helper (stylistic, touching a shared helper 8 other working readers depend on); bare module-level reader registration vs. a `_register()` wrapper (carried, unreachable — zero test call sites for `reset_record_readers_for_tests()`); no runtime `carrier` validation (the type system already covers every real call site); the `bridge/` guard's inherent cross-function and docstring-false-positive limits (an accepted heuristic-guard trade-off, mirroring existing codebase precedent); `UNJOURNALED_TABLES` including `journal_events` itself despite the spec's own "non-journal" phrasing (a spec-prose nit, not a code defect — fix would edit the spec); `ExpiredRecord.locator` mutability (carried, unreachable); nine readers' duplicated `None`→`ExpiredRecord` pattern (stylistic, each reason string is usefully domain-specific); the diff-size/no-second-review-pass concern (moot — this pass was that second review); DW-7 settled by neither of its own two originally-proposed branches (refuted — both were unsound once `enqueue()`'s real code was read; the diff's actual disposition is a better-reasoned third outcome, not a gap); "QA evidence lives in prose" (refuted — a `git diff` never embeds command transcripts by nature; verification was independently re-run and confirmed).

**Follow-up review recommendation: `true`.** Two medium-severity entries were patched in this pass (the upcaster tripwire's real-data gap, the coverage tripwire's one-directional staleness gap) — two or more medium patches on a first pass names this `true` per the workflow's own rule. Specific unverified risk: the patch round's new code (the real `MIN(cursor)` integration test, the `register_upcaster` validation guards, the stale-table test, the two DEBUG-log additions) has been independently re-verified by me directly (full diff read, targeted suite, full `./scripts/tripwires.sh`, both green) but not yet examined by a fresh, independent reviewer-subagent pass looking specifically at this patch round's code, as opposed to the pre-patch implementation.

**Verification performed:**
- `uv run pytest tests/journal/ -q` (run independently by me, both before and after the patch round) → 207 passed (pre-patch), 211 passed (post-patch, 4 new), 0 failed either time.
- `./scripts/tripwires.sh` (run independently by me, both before and after the patch round) → TRIPWIRES PASS both times: 722→726 passed (4 new tripwire tests), 2 skipped, ruff findings 29 (baseline ≤35, unchanged), mypy errors 57 (baseline ≤65, unchanged).
- Full diff read directly by me (not taken from either subagent's self-report) both before and after the patch round; every one of the 6 patched fixes re-verified against the actual patched source via direct `git diff`/`Read`, not trusted from the patch-round agent's own report.
- Matrix Test Audit: all 9 I/O & Edge-Case Matrix rows covered by at least one passing test that ran, confirmed by direct reading of the diff's test files (`test_coverage_tripwire.py`, `test_upcaster_tripwire.py`, `test_record_readers.py`).
- 4 independent review-layer subagents (blind-hunter, edge-case-hunter, verification-gap, intent-alignment) launched fresh against the full diff; every one of their 27 combined findings individually re-verified by me against the actual code before a verdict was rendered.
- Checked for a live gateway/core/scheduler process before and after every verification run — none running in this session's environment; did not start or restart the live production process at any point, per this dispatch's explicit instruction.

**Residual risks:** the 2 deferred items above (record readers have no live consumer pending `bridge/`; AD-4's prune-window tripwire pending Story 2.11, already tracked by DW-17); DW-7 remains genuinely open (a call-site-level journal-coverage gap the table-granularity coverage tripwire structurally cannot detect, on a live production task-execution hot path this story deliberately did not touch — see `deferred-work.md` DW-7's updated entry); the follow-up-review-recommendation risk named above (this specific patch round's code not yet independently re-reviewed by a fresh subagent pass).
