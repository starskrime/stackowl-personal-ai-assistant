# Decision Log — StackOwl De-complication PRD

## 2026-07-01 — Session decisions (locked with owner via AskUserQuestion, this session)

1. **LangGraph replaces asyncio** as the canonical pipeline backend. Owner explicitly reversed the original proposal (delete LangGraph): "i want asyncio to be replace by langraph". Asyncio backend is deleted only after parity + soak.
2. **Persistence give-up judge narrowed** — LLM judge runs only on suspicious turns (failed tool / refusal-shaped draft / empty draft). Structural veto + never-empty floor stay always-on. Owner approved "Narrow it (Recommended)".
3. **Proactivity delivery-time gate** — always generate proactive content; undeliverable routes to existing UndeliveredOutbox, flushed on next contact over any channel. Channel-agnostic owner identity added to settings. Owner approved.
4. **skill_synthesizer: wire counters** (not park). Note discovered post-decision: `success_rate` is already fed by LS7 `_update_skill_success_rates` (asyncio_backend.py:474); only `n_executions` increment is missing. Scope shrinks accordingly.

## Defaults confirmed by owner at implementation kickoff (2026-07-01, fresh session)

5. **Keep `parliament_step` stub** — CONFIRMED. With LangGraph canonical, its `Send` fan-out edge is the natural home for future multi-owl parliament.
6. **`acceptance_llm.py`** — REVERSED from PRD default: owner chose DELETE NOW (was "keep", flag-OFF/small). Add to FR-21 dead-code removal: delete `pipeline/acceptance_llm.py` (`LlmAcceptanceDeriver`), remove its call site in `pipeline/backends/asyncio_backend.py::_derive_turn_acceptance` (settings.acceptance_tier branch), and the `settings.acceptance_tier` field if it has no other reader.
7. **FR-22 instincts docs** — CONFIRMED clarify/re-scope (not delete): edit CLAUDE.md's "Instincts" bullet + `src/instincts/engine.ts` table row to state they describe the archived v1 TypeScript app under `old/`, not the current Python system.

## Audit corrections vs. initial findings

- `_drain_next` lock-across-await (orchestrator.py:1614) is a documented §4.3 race fix, by design — NOT an issue. No action.
- Learning-job starvation is bounded (`_MAX_DEFER_SEC = 900`, scheduler/scheduler.py:35) — chronic slippage, not total starvation.
- "registered ≠ reachable" historical disease is CURED — all learning subsystems are wired; the problem is damping/over-hedging, not dead wiring.

## Finalization — 2026-07-01

- Reviewer gate: single rubric-walker subagent → READY-WITH-FIXES (`review-rubric.md`). All findings applied: FR-9 mechanical bypass rule specified (<200 chars, 30-min window, no direct address — [ASSUMPTION]-tagged constants), FR-14 soak exit = 7 clean days with two named jq queries, Glossary gained LS7 + F088, FR-8 constant fixed at 200, FR-16 when-in-doubt→consent, FR-22 v1-TS scope caution, §0 dotfile note.
- Editorial polish skills (structure/prose) skipped — internal stakes, reviewer covered structure; content over polish per owner's speed request.
- `status: final` set. Handoff: fresh implementation session; suggested next skills: `bmad-create-epics-and-stories` or direct `bmad-dev-story`/subagent-driven implementation from PRD §8 order.

## Provenance

- Source plan: /home/boss/.claude/plans/i-want-you-reading-reactive-blanket.md (full copy in addendum.md).
- Audits: live-turn-pipeline audit, learning/proactivity audit, structural audit (3 Explore agents), design pass (Plan agent, verified constants against source), direct read of both backend files.
