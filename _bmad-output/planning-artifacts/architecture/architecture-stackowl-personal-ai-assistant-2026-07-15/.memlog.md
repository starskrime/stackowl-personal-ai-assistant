---
scope: Owl DNA Self-Improvement Lifecycle (PRD Features 1-7)
purpose: build-substrate
altitude: feature
updated: 2026-07-15T17:49
---

- (direction) Fast path adopted (user signaled via repeated 'continue') - drafting from PRD+addendum directly, [ASSUMPTION] tags for anything not explicitly settled
- (decision) Paradigm = Governed Pipes-and-Filters: propose -> clamp/govern -> validate(shadow-gate) -> commit -> observe. Formalizes evolution.py's existing implicit flow, extends with Feature 4's validate stage
- (decision) AD-1: every learned-artifact mutation (DNA or skill) MUST flow through the staged pipeline; no caller writes directly to owl_dna/skill storage. Binds all features.
- (decision) AD-2: LearningArtifactStore is the SINGLE versioning primitive for DNA+skills; DNACheckpointer and record_skill_mutation's internals are superseded, not duplicated. Binds Feature 1.
- (decision) AD-3: the shadow-validation gate is the ONLY path that may promote a checkpoint to live storage; nightly batch AND evolve_now both call it, neither may bypass. Binds Features 4,5.
- (decision) AD-4: signal-strength tiering computes the allowed delta BEFORE bound_dna()'s hard clamp; tiering only narrows, never widens bound_dna()'s ceiling. Binds Feature 3.
- (decision) AD-5: evolve_now routes EXCLUSIVELY through evolution_prompt.py's LLM-fallback path; may never invoke DnaAttributor's statistical path regardless of sample count. Binds Feature 5.
- (decision) AD-6: decay_rate_per_week gets IMPLEMENTED (weekly exponential decay toward authored baseline for traits unreinforced in-window), not deleted -- a real stability mechanism, not dead weight. [ASSUMPTION: user has not explicitly re-confirmed this at architecture level; flagged for review]
- (decision) AD-7: Feature 7 cross-signal is ADVISORY-ONLY in both directions (skill success_rate as an additional DnaAttributor input signal; DNA traits as an advisory weight on skill retention) -- never a gating condition on either subsystem's existing rules. Exact mechanism deferred to story-time (per PRD review finding).
- (event) Adversarial self-check: closed one gap (SignalStrength enum single-source-of-truth, added to Consistency Conventions). Spine finalized, status=final
- (decision) Post-finalize amendment (epics-and-stories elicitation): AD-3 tightened -- gate N/sample-size config is single-shared, not per-caller, closing a letter-vs-intent loophole found via Inversion+Red Team. AD-3 rule text updated in place, AD id unchanged.
