# Week 1 Delta Report — Atlas Pass

**Date:** 2026-05-16 (expedited — completed in single session)
**Pass:** Week 1 / Atlas — Breadth-first inventory
**Status:** COMPLETE

---

## What Was Done

### Infrastructure Built
- `_bmad-output/system-atlas/` directory structure created (components/, subsystems/, flows/, atlas/, reports/, scripts/)
- `scripts/build-manifest.ts` — auto-generates `components.json` from `src/` filesystem walk
- `scripts/gen-stubs.ts` — creates stub MD per component from manifest
- `scripts/coverage.ts` — coverage reporter with wiring analysis
- `README.md` — atlas entry point + usage guide
- `glossary.md` — comprehensive platform vocabulary (40+ terms, subsystem classification table)

### Inventory Results

| Metric | Value |
|---|---|
| Total .ts files in src/ | 757 |
| Manifest entries | 757 |
| Unmapped gap | **0** — 100% coverage |
| Stub MDs generated | 757 |
| Wired to message path | 55 (7%) |
| Side-systems (callers>0, not wired) | 677 (89%) |
| Orphans (0 callers) | 26 (4%) |

### Subsystem Classification — Surprises

Most important discovery: **OSCAR (`src/oscar/`, 33 files) is a fully-implemented GUI automation engine** — macOS screen capture, accessibility APIs, OCR + YOLO visual recognition, episodic/semantic/procedural memory, multi-oscar parliament, failure recovery. Wired via `ComputerTool` → tool registry. Not dead code.

**Forge and Tournaments confirmed ASPIRATIONAL** — never instantiated, no tests, type-references only.

**Most active unlisted subsystems:**
- `goals/` — heavily wired critical working memory (GoalGraph injected into system prompt)
- `background/` — 5-minute tick orchestrator for desire/memory/proactive jobs
- `triage/` — critical 4-path message router (DIRECT/AGENTIC/DELEGATE/PARLIAMENT)
- `knowledge/` — semantic knowledge graph, core reasoning infrastructure
- `signals/` — ambient context collection, wired to context pipeline
- `swarm/` — parallel specialist coordination, used in escalation path

**Side-systems that could be wired** (instantiated but not on message path):
- `quests/` — gamified learning (wires to continuity signals, could trigger on session end)
- `socratic/` — per-session mode (could wire to instinct system)
- `collab/` — multi-participant sessions (could wire to channel adapter detection)

---

## Top 10 Orphan Candidates (0 external callers)

These are flagged for Week 3 dead-code audit:

1. `src/clarification/` — 3 orphan files (clarification subsystem has multiple files with 0 external callers)
2. `src/engine/` — 1 orphan file (some engine files not imported from main ReAct loop)
3. `src/cli/` — 6 orphan files (CLI components possibly unused or feature-flagged)
4. `src/gateway/` — 1 orphan file
5. `src/skills/` — 1 orphan file
6. `src/owls/` — 1 orphan file
7. `src/parliament/` — 1 orphan file
8. `src/intelligence/` — 1 orphan file
9. `src/context/` — 1 orphan file
10. `src/infra/` — 1 orphan file

*Note: "orphan" = 0 callers by filename grep. Some may be entry points (no caller needed), test-only, or false negatives from dynamic imports. Week 3 audit will verify.*

---

## Critical Finding: Instinct System Has No Definitions

Zero instinct `.md` files exist in any `workspace/owls/*/instincts/` directory. The instinct engine (V1 LLM-based + V2 heuristic fast-path) is fully wired and works — but has nothing to fire. This is a high-impact gap identified pre-atlas. Week 3 will score and propose remediation.

---

## Critical Finding: Context Pipeline NOT Wired to Instinct Evaluator

The 30+ ContextPipeline layers (memory, preferences, pellets, learnings, frustration signals, session depth, continuity class, active tasks) produce `memoryContext` which goes to the passive system prompt background. The instinct evaluator runs separately with only `(owlName, userMessage)`. These two pipelines never share data. Result: instincts can only react to raw message text, not to rich behavioral context.

---

## Blockers

None — Week 1 completed ahead of schedule.

---

## Week 2 Goals

**Wires Pass** — trace every message-type hot path and build Mermaid sequence diagrams:

1. Trace CLI message end-to-end (cli-v2 adapter → GatewayCore → instinct eval → context pipeline → OwlEngine ReAct → provider → response)
2. Repeat for Telegram, A2A, Web adapters
3. Generate `flows/cli-message.md`, `flows/telegram-message.md`, `flows/a2a-message.md`
4. Depth-fill component MDs for every node on the hot paths (upgrade from `stub` → `mapped`)
5. Deep dive: `gateway/core.ts` section-by-section (4,911 lines — document by logical section)
6. Deep dive: `engine/runtime.ts` ReAct loop, `context/pipeline.ts` layer DAG

**Carry-forward:** Begin subsystem rollup MDs (`subsystems/gateway.md`, `subsystems/engine.md`, etc.)

---

## Coverage

```
Coverage (stub):   100% (757/757)
Coverage (mapped):   0% (0/757)
```

Week 2 target: mapped ≥ 15% (the hot path + critical subsystems)
