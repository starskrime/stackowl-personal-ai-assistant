ITERATION 2 — ADR-1 COMPLETE [x], flag ON in prod. Next: ADR-4. — 2026-06-27
ADR-1 shipped across feat/adr-1-acceptance-authority (substrate @0172e6bf) + feat/adr-1-effect-migrations
(send_* DeliveryAck). Authority is the single decider; the ≥6 proxies delegate (read the one `verified`
the authority writes). Invariant tested per kind (text/delivery/artifact/http): success⟺measured, self-stamp
ignored. send_message/send_file declare DeliveryAck (transport ack→authority→verified). Flag
settings.acceptance_authority ON in /home/boss/.stackowl/stackowl.yaml (top-level). Closures logged in
.ralph/BACKLOG.md "ADR implementation log". DISCOVERY: effect seams already self-verify from S1-S4, so ADR-1's
value is UNIFICATION; remaining per-tool post_condition adoption is optional incremental hardening (BACKLOG).

GATES: 75 send+authority + 112 proxy/acceptance + 34 objectives green; ruff clean; mypy clean on changed
(2 send Notification.target_chat_id errors PRE-EXIST on clean main — git-stash-confirmed, do NOT chase).
Full tools+pipeline combined run HANGS = pre-existing I/O block (obs 7522) → gate TARGETED suites only.

NEXT ITERATION = ADR-4 (Reachability invariant at boot). Read .ralph/adr/ADR-4-reachability-invariant.md +
ROOT_CAUSES.md T4. Shape: health/reachability/census.py run_census/census_passes EXIST but only test callers;
StartupOrchestrator.run never calls them (F-86 the census eats its own tail). Make it a BOOT PHASE that fails
loud. Closes F-45, F-76, F-77, F-78, F-86 (⤷F-87). Flag-gated default reproduces today (census advisory/off),
ON = fail-closed. Branch feat/adr-4-reachability-invariant off main. TDD: a registered-but-unreachable
capability must fail the boot invariant (test fails on pre-ADR code).
Known pre-existing reds: IMPLEMENT_PROMPT §6 — git-stash-verify any NEW red vs clean main.
