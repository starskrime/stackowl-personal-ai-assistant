ITERATION 13 — ADR-5 KEYSTONE shipped (merged to main), ADR-5 still [ ] (move-3 remains). — 2026-06-27
Done: ADR-1/4/2/3 [x]. ADR-5 = trustworthy/verified-gated learning. RECON FINDING (research-before-
building): MOVE 1 was ALREADY enforced — ADR-1/B4b @asyncio_backend.py:336-340 collapses a verified=False
effect (consequential_failures / refuted AcceptanceVerdict) into BOTH success=trustworthy AND
failure_class="unachieved_effect"; ALL learners (miner tool_outcome_miner.py:112 `if o.failure_class:
continue`, critic outcome_store.py:176, reflection reflection_store.py:76, dna_attribution.py:154) already
gate on failure_class IS NULL. So "mine only MEASURED success" needs NO new code — there is NO `verified`
column; failure_class="unachieved_effect" IS the persisted proxy for verified=False. Locked with invariant
test (verified-false outcome never mined).
SHIPPED THIS ITER (flag settings.trustworthy_learning default OFF=byte-identical):
  - MOVE 1: invariant test only (already-enforced). F-26 source gate.
  - MOVE 2 (F-50): classify._gather_recent_reflections now calls the CODED-BUT-DEAD
    ReflectionStore.semantic_for_owl (reflection_store.py:266) when flag ON + query + embedding_registry
    present; recency-only recent_for_owl when OFF. semantic_for_owl self-degrades to recency on embed
    failure → ON safe on any box. Caller @classify.py:575 now passes query=state.input_text.
    (F-46 mean_quality-in-ranking ALREADY DONE @heuristic_ranking.py:82 live via classify.py:436.)
  5 ADR-5 tests @tests/learning/test_adr5_trustworthy_learning.py; learning 26 + classify 18 green; ruff+mypy clean.

★REMAINING for ADR-5 [x] (next iteration, then ask owner to flip trustworthy_learning ON):
  - MOVE 3 (F-43/72): ephemeral WITHIN-TURN failed-approaches scratch. PipelineState has NO scratch field
    (state.py:242 has `no_progress_tools` tuple — mirror it: add `failed_approaches: tuple[str,...]=()`).
    Loop records a failed approach @execute.py:1188 (the `else` non-trustworthy branch; circuit breaker
    already bounces same-tool after threshold=3). Add a consult before re-trying. NOTE: loop failure
    tracking currently lives in TurnProgressTracker/ContextVars, not PipelineState — decide where scratch lives.
  - F-4 decision-time learned-heuristic consult @execute.py:1210 is DELIBERATELY DEAD (latency + weak-model
    amplification safety; guarded by tests/learning/test_heuristic_demote.py). The code note says re-surfacing
    needs its own design. DO NOT blindly re-add — honor the guard; ADR-5 doesn't resolve the amplification risk.
    find_for_tool ORDER BY evidence_count (tool_heuristic_store.py:124) would need quality if re-wired.
BUILD ORDER REMAINING: finish ADR-5 move-3 → ADR-6 → ADR-7.

ITERATION 12 — ★ADR-3 COMPLETE & MARKED [x]. — 2026-06-27
Done: ADR-1 [x], ADR-4 [x], ADR-2 [x], ADR-3 [x]. ALL 5 interaction gates now DELEGATE their
ask-vs-act decision to the ONE ReversibilityResolver (interaction/reversibility_resolver.py):
  1. clarify _resolve_default (F-68/69/71) — keystone; shared by tool pre-park + timeout + gateway pre-ask.
  2. objective _park_is_irreversible (F-44) — via must_reach_user == bool(consequential_failures).
  3. consent ConsentPolicy.request reversible→auto-allow tier (F-27) — must_reach_user(reversible)=False⇒auto-allow.
  4. cost_pause CostPauseGuard.gate continue-and-notify (F-70) — high_stakes=not under_cap⇒must_reach_user==under_cap.
  5. router/execute _maybe_clarify act-first-vs-park (F-3/56) — reversible iff _clarify_resolvable_from_context.
EACH delegation reproduces its gate's pre-ADR inline rule EXACTLY ⇒ flag ON is BYTE-IDENTICAL (pure
unification, ADR-2 pattern; the proactivity gains were already shipped as F-27/44/68-71 in prior arcs —
ADR-3's value is ONE authority replacing 6 scattered ad-hoc checks). FLAG settings.reversibility_resolver
flipped ON in code default (owner approved via AskUserQuestion — [[feedback_ask_before_flag_default]]).
KEY API: ReversibilityResolver.resolve(Decision)->Verdict(act,assumption,undo_handle) for default-picking
gates; must_reach_user(Decision)->bool (high_stakes or is_irreversible) for binary escalate-or-not gates.
Reversibility tri-state via .reversible(via=)/.irreversible()/.unknown(). _most_likely mirrors clarify
_resolve_default EXACTLY. INVARIANT preserved: resolver only DOWNGRADES park→act, never act→park (a2a/
non-interactive fail-closed untouched). GATES GREEN with flag ON: 216 across interaction/consent/objective/
clarify/config; ruff+mypy clean. Health green (db/fs/graph + 3 tiers). Closures: F-3/27/44/56/68/69/70/71.
NOT live-restarted on a real turn yet (flag ON byte-identical so low risk; do a smoke next session).

NEXT ADR in build order: ADR-5 — Trustworthy (verified-gated) learning (feat/adr-5-trustworthy-learning).
  Once ADR-1 makes success MEASURED, mining/recall gate on the `verified` signal + ephemeral within-turn
  failure-awareness. NEVER persists negatives (positive-only directive [[feedback_positive_only_learning]]).
  Depends on ADR-1 (done). Read .ralph/adr/ADR-5-*.md + ROOT_CAUSES. Closes F-26/43/46/47/48/50/51/54/72.
BUILD ORDER REMAINING: ADR-5 → ADR-6 → ADR-7.

ITERATION 11 — ADR-3 KEYSTONE shipped (merged to main), ADR-3 still [ ]. — 2026-06-27
Done: ADR-1 [x], ADR-4 [x], ADR-2 [x]. ADR-3 = ReversibilityResolver. Built the ONE authority +
declared tri-state signal, and routed the FIRST TWO gates (ADR-2 playbook: keystone + per-gate
incremental merge).

NEW: src/stackowl/interaction/reversibility_resolver.py —
  - Reversibility (frozen, tri-state via classmethods .reversible(via=)/.irreversible()/.unknown();
    is_reversible/is_irreversible/is_unknown props). `via` = undo-handle name (e.g. "undo_write").
  - Decision (reversibility, high_stakes, choices, default, interactive, context).
  - Verdict (act, assumption, undo_handle).
  - ReversibilityResolver.resolve(decision)->Verdict: park IFF must_reach_user OR no most-likely
    default; else act-on-assumption keeping the undo handle. must_reach_user(decision)->bool = the
    binary reversibility/stakes gate (high_stakes or is_irreversible), factored out for gates that
    only need escalate-or-not. _most_likely mirrors ClarifyGateway._resolve_default EXACTLY (one-item
    menu / consistent default / else None) → flag-on is a STRICT GENERALIZATION (byte-identical).
  - reversibility_resolver_enabled() fail-safe flag reader (pattern of base.py _acceptance_authority_enabled).
FLAG: settings.reversibility_resolver default OFF (byte-identical) — settings.py just after
  unify_gateway_recovery. Intended ON once all gates routed + green.

GATES ROUTED so far (delegate when flag ON; OFF=inline byte-identical; spy tests assert authority
consulted ON / NOT consulted OFF):
  1. clarify (F-68/69/71): ClarifyGateway._resolve_default delegates to resolve() → verdict.assumption.
     This ONE helper is already shared by clarify tool pre-park + graceful-timeout + gateway pre-ask,
     so routing it covers 3 call sites. Parametrized 8-row matrix proves byte-identical ON vs OFF.
  2. objective driver (F-44): ObjectiveDriverHandler._park_is_irreversible delegates to
     must_reach_user (consequential footprint→irreversible; clean park→reversible) == bool(consequential_failures).
Gate tests: tests/interaction/test_reversibility_resolver.py (33). Invariant test (fails pre-ADR,
passes now): test_reversible_low_stakes_with_clear_default_acts_and_retains_undo_handle.
GREEN: tests/interaction 91, tests/objectives 160-incl, pipeline clarify+overclaim+settings 28; ruff+mypy clean.

★REMAINING ADR-3 GATES (next iterations, then mark [x] + flip flag ON):
  - consent (F-27): tools/consent.py ConsentPolicy.request — the `reversible`→`reversible_auto` tier
    (consent.py:~233-237) should DELEGATE; reversibility already derived from manifest.commit_coupling
    =="transactional" (registry.py ConsequentialActionGate._is_reversible:~123) + undo_write token =
    the real `reversible_via`. Insertion: registry.py:105-115 where manifest ctx is in hand.
  - cost_pause (F-70): interaction/cost_pause.py CostPauseGuard.gate block-threshold (:207-229) →
    reversible spend under hard cap = act-and-notify via resolver.
  - router clarify verdict (F-3/56): owls/router.py emits `clarify` (prompt-only); execute.py
    _maybe_clarify (:1916) consumes — _clarify_resolvable_from_context (:1881) explicitly DEFERS to a
    structured signal = this resolver. a2a/non-interactive (state.py interactive=False fail-closed) must
    be PRESERVED — resolver only DOWNGRADES park→act, never act→park.
  - F-71 already covered via the clarify delegation.
NOTE: keep ConsentRequest.reversible / cost-pause block_threshold / etc. — make them DELEGATE, never delete.

BUILD ORDER REMAINING: finish ADR-3 gates → ADR-5 → ADR-6 → ADR-7.

ITERATION 4 — ADR-2 SUBSTRATE shipped (on main @b6645029), ADR-2 still [ ]. — 2026-06-27
Done: ADR-1 [x], ADR-4 [x]. ADR-2 substrate = pipeline/recovery_actuator.py (Failure +
classify_tool_failure + is_transient_result + RecoveryActuator.recover()/should_retry() +
RecoveryOutcome): one bounded ladder (retry→reroute→substitute→honest-surrender) re-verifying each
rung (ADR-1), consequential NEVER auto-retried, total/never-raises. execute.py _is_transient_failure
DELEGATES to actuator's is_transient_result (classifier relocated to the authority; byte-identical).
Tests tests/pipeline/test_recovery_actuator.py (8, fault matrix). 84 recovery/execute/substitution green;
ruff+mypy clean. 1 floor-invariant red = KNOWN pre-existing (_FakeResponse.usage drift, IMPLEMENT_PROMPT §6).

★GIT DEVIATION (honest): the ADR-2 substrate commit b6645029 landed DIRECTLY on main (I forgot to branch
after the ADR-4 merge left HEAD on main). Already pushed. NOT force-fixed — force-pushing main is the bigger
risk (prior landmine). Branch ref feat/adr-2-recovery-actuator created at b6645029 for traceability. GOING
FORWARD: branch FIRST every iteration (git checkout main && git checkout -b feat/adr-<n>-... BEFORE any edit).

UPDATE (iter 5, main @43de88a2): execute-loop now DELEGATES the retry DECISION too —
RecoveryActuator.should_retry(classify_tool_failure(...)) replaced the inline guard (byte-identical).
So the tool-dispatch path delegates to the authority END-TO-END (classifier + decision). ADR-2 still [ ]:
the NON-TOOL failure paths still surrender on their own. Branch-first was honored this time
(feat/adr-2-subsystem-routing).

UPDATE (iter 6, main @50213274): PROVIDER GATEWAY now routes through the actuator.
On a TRANSIENT provider fault, llm_gateway complete()/complete_with_tools() call
RecoveryActuator.recover() to retry the SAME tier once (incl. at ceiling) BEFORE cascading.
Flag provider_retry_same_tier_once default ON (owner chose ON via AskUserQuestion — see
[[feedback_ask_before_flag_default]]); flag-off byte-identical. Live-verified: flag ON, health
green (3 tiers). functools.partial used for the retry thunk (avoids B023 + lambda-inference).
NEW RULE THIS SESSION: ASK owner before setting any flag default — feedback_ask_before_flag_default.

UPDATE (iter 7, main @4df691d0): CHANNEL TRANSPORT now routes through the actuator.
ProactiveDeliverer.deliver() hands a 'failed' transport to recover() whose reroute rung re-sends
to notifications.fallback_channel (re-verifies 'delivered'). OPT-IN OFF by default (owner chose
OFF — empty fallback_channel = byte-identical); same-channel reroute skipped; never raises. F-64/65/66.
Tests in tests/notifications/test_deliverer.py. 66 notifications/recovery green.

ADR-2 routed so far: tool-dispatch (classifier+decision) + provider gateway + channel transport.
REMAINING (ADR-2 still [ ]): NEXT — route the remaining subsystems to RecoveryActuator.recover(), each
behind a flag (ASK owner for default per [[feedback_ask_before_flag_default]]), re-verifying each rung:
  1. providers/llm_gateway.py except-blocks (:128/:241) — hand provider faults to recover() w/ reroute=next-tier
     (closes F-16/17/18/21; note: cascade already mostly works via is_cascadable_fault — make it DELEGATE).
  2. channel adapters transport drop (F-64/65/66) reroute=alt-channel.
  3. scheduler _mark_failed exhausted jobs (F-60/62); objective driver blocked sub-goals replan (F-40/41);
     gateway lost in-flight turn replay (F-35/67/37).
  Then flip flag ON, live-verify (kill core mid-turn → turn replays), cite F-numbers, mark ADR-2 [x].
RECON MAP: obs 7681 + ROOT_CAUSES T2. execute.py 3-rung ladder is the actuator's tool-dispatch caller already.

UPDATE (iter 8, main @18ca092b): OWNER DECISION — recon CONFIRMED the 3 remaining ADR-2
subsystems (scheduler, objective driver, gateway turn replay) ALREADY have correct bounded
recovery satisfying the ADR-2 invariant (objective: _MAX_SUBGOAL_ATTEMPTS+cooldown requeue;
gateway: _inflight requeue + _MAX_REPLAY + _MAX_WEDGE_REDISPATCH; scheduler: STEER-5/F113).
They just don't DELEGATE to the one authority. Owner chose "COMPLETE LITERAL UNIFICATION"
(route all 3 through RecoveryActuator, byte-identical) + flag default ON for all 3 (both via
AskUserQuestion — see [[feedback_ask_before_flag_default]]). Pattern: route the retry DECISION
through RecoveryActuator.should_retry(Failure(...)) (mirrors execute-loop delegation); each
site keeps its own across-tick/restart EXECUTION (the actuator's synchronous ladder can't model
across-tick retry — the ADR says sites keep their rung handlers).
  DONE THIS ITER — OBJECTIVE DRIVER (F-40/41): driver._on_subgoal_failure retry gated by
  self._may_retry(reason) → RecoveryActuator.should_retry(Failure(kind="objective",
  transient=True, consequential=False)). Flag unify_objective_recovery default ON; OFF=inline
  byte-identical. Injectable recovery= for tests (spy asserts authority consulted when ON,
  NOT when OFF, both retry identically). NOTE: kept transient=True (NOT classified) on purpose
  — the F-43 different-approach retry means even a "deterministic" sub-goal failure is worth a
  re-attempt, so classifying it out would HARM resilience; the value here is one-policy +
  consequential-never-retried, not classification. 82 objectives/recovery/journey/config green;
  ruff+mypy clean. NOT live-restarted (provider box reachability TBD; no migration, flag ON in
  code default so no prod config flip needed).
UPDATE (iter 9, main @2b6b55de): SCHEDULER routed. scheduler._run_job retry-vs-terminal-fail
decision gated by self._may_retry(result) → should_retry(Failure(kind="scheduled_job",
transient=True, consequential=False)). Flag unify_scheduler_recovery default ON; OFF=inline
byte-identical. module-level _unify_scheduler_enabled() (monkeypatchable like llm_gateway).
STEER-5/F113 retry_at/retry_count execution UNCHANGED. Scheduler+recovery suites green.

UPDATE (iter 10, main @b45b6c24): GATEWAY routed — ADR-2 COMPLETE. Two decision seams behind
flag unify_gateway_recovery (default ON; OFF=inline byte-identical):
  - GatewayLink._flush_pending (F-38): replay retry-vs-surface → self._may_retry_replay(exc).
  - TurnRegistry._redispatch_wedged_goal (F-67): re-dispatch-vs-give-up → self._may_redispatch().
Both → should_retry(Failure(kind="gateway_turn", transient=True, consequential=False)). F-35
unconditional in-flight requeue UNCHANGED. Each module has its own _unify_gateway_enabled()
reader (monkeypatchable). 212 gateway/runtime/recovery green (incl. existing F-38/F-67/replay/
inflight = byte-identical proof); ruff+mypy clean.

★ADR-2 MARKED [x]. ALL 6 subsystems delegate to ONE RecoveryActuator authority:
  execute.py (tool) / llm_gateway.py (provider) / deliverer.py (channel) / objectives/driver.py
  (objective) / scheduler.py (scheduler) / gateway_link.py + turn_registry.py (gateway turn replay).
  All flags ON in code default (no prod config flip needed; no migration). Invariant: no call site
  surrenders a recoverable failure on its own before the ladder; recovered = ADR-1-verified.
  NOT YET live-verified end-to-end (kill core mid-turn → turn replays) — the headline live test;
  do it next session if the platform is running. Health green (db/fs/graph + 3 provider tiers) @iter8.

NEXT ADR in build order: ADR-3 — ReversibilityResolver (feat/adr-3-reversibility-resolver).
  One reversibility/stakes signal + default-resolution authority so trivial reversible decisions
  resolve act-first instead of bouncing to the user. Closes F-3/27/44/56/68/69/70/71. Does NOT
  depend on ADR-2. Read .ralph/adr/ADR-3-*.md + ROOT_CAUSES T3. Note: F-44 (reversible park
  auto-resolve) already partly done in objective driver (_park_is_irreversible) — recon first.

BUILD ORDER REMAINING after ADR-2: ADR-3 → ADR-5 → ADR-6 → ADR-7.
Known pre-existing reds: IMPLEMENT_PROMPT §6 — git-stash-verify any NEW red vs clean main.

ITERATION 14 — ADR-5 MOVE 3 shipped → ADR-5 MARKED [x] (merged to main, flag ON default). — 2026-06-27
SHIPPED: ephemeral within-turn failed-approach scratch @execute.py (F-26/43/72). _run_with_tools gains a
loop-local failed_approaches:set[str], filled in the non-trustworthy result branch (where
record_no_progress already runs), consulted at the TOP of _dispatch. When the model blindly RE-ISSUES the
EXACT (tool, args) approach that already failed this turn → _repeated_approach_refusal (CONTAINMENT: no
TOOL_FAILED_MARKER, records nothing → cannot trip give-up floor, mirrors circuit-breaker bounce). Helpers
_approach_signature (canonical JSON, never raises) + _repeated_approach_refusal at module level.
  KEY SEMANTICS: consult is at _dispatch top, NOT _guarded_dispatch — the recovery ladder's internal
  retries re-enter _guarded_dispatch, so a transient that self-heals is NEVER blocked; only a MODEL-issued
  blind repeat is. Finer than the by-name circuit breaker (exact args, fires on FIRST repeat).
  RESEARCH-BEFORE-BUILDING: found existing F-26 advisory @registry.py:336 (_advise_on_prior_failures) — but
  it's by-NAME + LOG-ONLY + never surfaces to model/skips run. MOVE 3 is complementary (exact-approach +
  acted-on), not a dup. Registry advisory stays.
  Flag-gated trustworthy_learning: OFF ⇒ _approach_sig="" (signature not even computed) ⇒ set never
  consulted/filled ⇒ byte-identical, zero hot-path overhead.
OWNER DECISION (AskUserQuestion): flip trustworthy_learning default → ON (all 3 moves shipped). settings.py
default True; OFF = pre-ADR recency-only recall + no scratch (also byte-identical). NOT live-verified
end-to-end (semantic recall self-degrades + scratch is containment-only ⇒ ON safe on any box).
  5 MOVE-3 tests @tests/pipeline/test_within_turn_scratch.py: repeat-bounced(ON) / re-executes(OFF
  byte-identical) / different-args-not-bounced / successful-approach-not-recorded / bounce-records-no-
  effectful-failure(P0). circuit_breaker+dispatch+learning+config+classify suites green; ruff+mypy clean.
★ADR-5 MARKED [x]. main @49e830fd (MOVE 3 merge) + flag-flip commit. NOT pushed yet.
BUILD ORDER REMAINING: ADR-6 (closed detect→heal→verify lifecycle) → ADR-7 (DecisionLedger).
NEXT ADR: ADR-6 — feat/adr-6-closed-loop-lifecycle. Boot/health/supervisor/restart DRIVE recovery toward a
goal and re-verify, not just observe. Heal step depends on ADR-1/ADR-2 (both done). Read .ralph/adr/ADR-6-*.md
+ ROOT_CAUSES. Closes F-36/39/73/74/85/87/88.

ITERATION 15 — ADR-6 KEYSTONE shipped (closed detect→heal→verify loop), ADR-6 still [ ]. — 2026-06-27
RESEARCH-FIRST: all lifecycle pieces exist UNWIRED — health/aggregator.py (detect), health/contributors.py
(ResilienceContributor wraps HealableResources), infra/resilience.py (HealableResource.ensure_available =
the recycle primitive + retry_once_on_dead_handle), service/watchdog.py, supervisor/supervisor.py,
scheduler/handlers/health_sweep.py (the S2 seed = detect+ALERT only, F-87 deferred recycle). ADR-6 CONNECTS
them, doesn't rebuild. The health_sweep handler IS the HealthLoop seed.
SHIPPED THIS ITER (flag settings.health_loop default OFF=byte-identical) @efca20ab on feat/adr-6-closed-loop-
lifecycle: HealthSweepHandler closed loop. After detect+split, _heal_and_verify recycles each down/degraded
subsystem that has a registered HealableResource (healers dict, injected; NONE today→no-op even ON), retry
DECISION routed through ADR-2 RecoveryActuator.should_retry(Failure(kind="health",transient=True,
consequential=False)); then RE-COLLECT (verify) → recovered=attempted−still_unhealthy; recovered subsystems
log "recovered" + DON'T alert; still-down → existing escalate. New ctor params healers/recovery (both
optional). Module-level _health_loop_enabled() monkeypatchable. _heal_and_verify never raises (heal fail →
stays unhealthy → escalates).
  4 tests @tests/scheduler/handlers/test_health_loop.py (healed+reverified→no-alert / unhealable→escalate /
  flag-OFF detect-only byte-identical / ON+no-healer detect-only). +6 existing health_sweep + 204
  scheduler/config green; ruff+mypy clean.
★REMAINING for ADR-6 [x] (next session — then ask owner to flip health_loop ON per feedback_ask_before_
flag_default):
  - F-87 FULL HEAL: thread LIVE HealableResource refs (DbPool @db/pool.py, providers @providers/base.py,
    browser runtime) from the serve process into assembly's `healers=` map (assembly.py:306 currently passes
    only aggregator+alert; _build_health_aggregator uses config-probe contributors, NOT the live healables).
    This is the "larger wiring change" the F-87 docstring deferred. Until done, the loop is a no-op even ON.
  - F-85 watchdog: gate WATCHDOG=1 ping @service/watchdog.py on the loop's liveness verdict (withhold ping
    when a critical subsystem is wedged-but-alive → systemd restarts).
  - F-73/74/36 supervisor: gate @supervisor/supervisor.py restart on a progress/verify signal (no blind
    restart; respawned core must be reconnect-verified).
  - F-39 crash user-notice; F-88 run loop in mono role.
BUILD ORDER REMAINING: finish ADR-6 seams → ADR-7 (DecisionLedger, largely downstream).

ITERATION 16 — ADR-6 SEAM F-87 shipped (live HealableResources wired into the heal loop). — 2026-06-27
The keystone heal step was a no-op even ON: HealthSweepHandler.healers was empty. SHIPPED @main 2916866e:
SchedulerAssembly.build() now builds the healers map from the LIVE serve-process resources keyed by health-
status name — {"db": DbPool} + {f"provider:{p.name}": p for provider_registry.all()}; +{"browser":
CamoufoxRuntime} AND a live BrowserContributor on the aggregator, both gated on settings.health_loop so the
DETECT set is unchanged when OFF. New build() param browser_runtime: HealableResource|None; orchestrator
threads the live runtime (None in gateway role). Flag OFF byte-identical: heal short-circuits on
_health_loop_enabled() → map never consulted; no browser contributor added.
  LANDMINE (memory project_skills_live_path_census): Settings has extra="ignore" → Settings(health_loop=True)
  SILENTLY DROPS the kwarg (verified False). Tests set it via .model_copy(update={...}). Production loads from
  config so real serve is unaffected.
  2 wiring tests @tests/scheduler/test_scheduler_assembly.py (db+provider healers present, browser excluded
  OFF / included+contributor-registered ON). 15 assembly + 10 health_loop/sweep + 8 wiring-audit green;
  ruff+mypy clean on changed src (pre-existing reds: B017 in frozen-components test; orchestrator mypy
  1535/2009 — both pre-date this change). code-reviewer APPROVE, all 4 invariants hold.
  NOTE: detect/heal object mismatch is inherent to F-87 — db detect is a path-probe (DbContributor), heal is
  the live DbPool; recycle is idempotent/safe so the mismatch is harmless (probe down→recycle pool→re-probe).
★REMAINING for ADR-6 [x] (4 seams, next sessions):
  - F-85 watchdog: gate WATCHDOG=1 ping @service/watchdog.py on the loop's liveness verdict (withhold when a
    critical subsystem is wedged-but-alive → systemd restarts).
  - F-73/74/36 supervisor: gate @supervisor/supervisor.py restart on a progress/verify signal; respawned core
    reconnect-verified (no blind restart).
  - F-39 crash user-notice; F-88 run loop in mono role.
  After these → ADR-6 [x], ask owner to flip health_loop default ON (feedback_ask_before_flag_default), then
  ADR-7 (DecisionLedger).

ITERATION 17 — ADR-6 COMPLETE [x] (F-39 shipped; F-85/73/74/36/88 found already-done). — 2026-06-27
RESEARCH-FIRST WIN: of the 4 "remaining" seams in the iter-16 handoff, only F-39 was genuinely unbuilt —
the others shipped in prior reliability arcs:
  - F-85 watchdog liveness gate: DONE+tested. service/watchdog.py has liveness_check/_is_live/ping-skip;
    orchestrator wires _build_liveness_aggregator (db pool + data/log dirs ONLY — provider outage must NOT
    kill the process), gated on down-only (degraded still serves), fails OPEN. tests/service/
    test_watchdog_lifecycle.py + tests/health/test_aggregator_liveness.py.
  - F-73/74: DONE (supervisor.py _record_failure escalate+restart, max-failures floor, F-75 spin guard;
    refactored @obs 7482 earlier today).
  - F-36: DONE (orchestrator _supervise_core 185-241 — respawned core reconnect-verify bounded by
    _CORE_BOOT_TIMEOUT_S, LOUD failure + stop_event on timeout, first_conn_event cleared before respawn).
  - F-88: DONE (orchestrator detect_service_manager + loud warn on None bare-shell; obs 7442).
SHIPPED THIS ITER @main cb280bb3 (F-39 crash user-notice): _supervise_core gains optional on_crash:
Callable[[int|None],Awaitable[None]]; fired on each unexpected core exit (AFTER the stop_event guard → no
false notice on intentional shutdown). Gateway call site wires a closure delivering a CRITICAL proactive
Notification via the existing deliverer ("StackOwl restarted after an unexpected core exit…"); None when no
deliverer. Fail-safe: notice error logged, never breaks the respawn loop. Repeat notices throttled by the
existing respawn backoff (1s→30s). brief_channels hoisted out of the closure (mypy: self._settings is
Settings|None, not narrowed inside nested def). 1 test @tests/startup/test_resilience_findings.py
(crash→on_crash fires once with rc). 14 green; ruff clean; mypy unchanged (3 pre-existing: 1093/1546/2020).
★ADR-6 MARKED [x]. All 7 findings closed (F-36/39/73/74/85/87/88). OWNER DECISION (AskUserQuestion):
health_loop default stays OFF for now — ship code-complete, flip in prod config when ready (not yet live-
verified end-to-end on a real box). Flag OFF = byte-identical. [[feedback_ask_before_flag_default]].
NEXT ADR: ADR-7 — DecisionLedger (feat/adr-7-decision-ledger). Read .ralph/adr/ADR-7-*.md + ROOT_CAUSES.
Largely downstream of 1-6. This is the FINAL ADR — after it the 7-ADR Jarvis arc is complete.

ITERATION 18 — ADR-7 STARTED: DecisionLedger infra shipped (step 1/3). — 2026-06-27
SHIPPED @main 862b1d2d. Research-first: the turn-scoped ContextVar ledger idiom already
exists twice (infra/recovery_context.py, infra/tool_outcome_ledger.py) — mirrored it
EXACTLY, no new abstraction. New src/stackowl/infra/decision_ledger.py:
  Decision{point, verdict, reason, inputs, alternatives_considered, evidence} (frozen) +
  bind()/reset()/record_decision()/get_decisions(). Unbound turn → record no-ops (logged).
Settings flag decision_ledger: bool = True. Both backends (asyncio + langgraph) bind a
fresh ledger per turn GATED on settings.decision_ledger (OFF → bind skipped → record
no-ops → byte-identical; the ADR-7 rollback). Gate uses self._services.settings; None
(unit tests) → bind (default-ON posture). reset in finally behind `if token is not None`.
  6 infra tests (tests/infra/test_decision_ledger.py) + 10 backend + 37 config green;
  ruff+mypy clean on all changed files; code-reviewer APPROVE (4 invariants hold).
★REMAINING for ADR-7 [x] (2 steps, next sessions):
  STEP 2 — wire emitters: each ADR-1–6 authority calls record_decision at its verdict
    point: AcceptanceAuthority (accepted/why @pipeline/acceptance_authority.py),
    RecoveryActuator (rungs tried/surrendered @pipeline/recovery_actuator.py),
    ReversibilityResolver (act-vs-ask @interaction/reversibility_resolver.py),
    LearnedContext (which heuristic steered + provenance @pipeline/lesson_context.py),
    router/classifier (verdict + confidence). Gate-free at emit sites (no-op when unbound).
  STEP 3 — point consumers at the ledger: recovery_summary reads ledger (closes F-9/10),
    crash path emits a crash Decision + user notice (F-39 already ships notice — add the
    Decision row), next-step signal (F-28) → Decision kind=next_step, plus an /explain-style
    surface that READS the ledger ("why did you do that?" = a read, not a confabulation).
  VERIFY (ADR §Verification): a turn that recovered + acted-on-assumption + was steered by a
    heuristic produces a ledger with all three Decisions+reasons.
  After step 3 → ADR-7 [x] → 7-ADR Jarvis arc COMPLETE. Flag already ON (incremental;
  empty/unread until emitters land, so ON is safe now).

ITERATION 19 — ADR-7 STEP 2a: ADR-1/2/3 authority emitters shipped. — 2026-06-27
SHIPPED @main 25972f64. AcceptanceAuthority/RecoveryActuator/ReversibilityResolver each emit
ONE typed Decision at their verdict point via a thin public wrapper over the existing logic
(renamed _observe/_recover/_resolve; zero behavior/log change; emit gate-free → no-op when
ledger unbound → byte-identical OFF). acceptance emits only on a real opinion (accepted not
None). recovery records rungs_tried as alternatives_considered + recovered/surrendered. revers.
records act/ask + assumption/undo. Verify test tests/pipeline/test_decision_ledger_emitters.py
(3) + 146 authority tests green; ruff+mypy clean.
PRE-EXISTING FAILURE (NOT mine, execute.py untouched): test_execute_floor_invariant::
test_execute_threads_residual_deadline_into_provider — wrapup_deadline_s computes None, fails
in isolation. Flag for a separate fix.
★REMAINING for ADR-7 [x]:
  STEP 2b — emitters: LearnedContext (which heuristic steered + provenance @pipeline/
    lesson_context.py) + router/classifier (verdict + confidence). Same gate-free emit pattern.
  STEP 3 — consumers READ the ledger: recovery_summary (F-9/10), crash Decision row (F-39),
    next_step Decision (F-28), /explain surface ("why did you do that?" = a read).
  VERIFY (full): a turn that recovered+acted-on-assumption+steered-by-heuristic → ledger has
    all 3+reasons (current test covers the first two; add heuristic after 2b).
  After step 3 → ADR-7 [x] → 7-ADR Jarvis arc COMPLETE.
