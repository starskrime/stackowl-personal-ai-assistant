---
scope: The StackOwl Bridge: the approved agentic-OS web dashboard (ship bridge) replacing control_plane — live event stream, authorised web control path, owner identity and access, front end, voice channel
purpose: build-substrate for bmad-create-epics-and-stories and bmad-loop
altitude: feature
updated: 2026-09-13T08:52
---

- (event) Run started 2026-09-12: Coaching path, spine-only deliverable (owner choice via interactive question).
- (constraint) Source of truth docs/agentic-os-dashboard/full-picture.md approved 2026-09-12 (Q1-Q52, L1-L4); principles §2 bind: truthful motion, dark-cockpit attention, no back door, one mind many surfaces, text never only in canvas, permissive licences, self-hosted (named DNS/ACME fallback exception), any hardware, retired means deleted.
- (constraint) Approved UX contract: planning-artifacts/bridge-mockup BRIEF-identity-and-engine.md + BRIEF-helm-dial-v2-approved.md (Helm Dial v2 won).
- (constraint) Owner rules: one task loop never a second engine; durable state md or sqlite only, one copy; DB changes via idempotent migrations only; all paths via StackowlHome; fix the platform not this setup; everything ships enabled; 4-point logging; no silent catch; no vendor names in src; no hardcoded keyword lists; cross-platform.
- (constraint) Q45: old control_plane is deleted in the change that ships the bridge, not before; its Q29 setup-code ownership mechanism is reused for passkey enrolment (Q41).
- (constraint) Q43: build step and libraries allowed; every asset vendored and platform-served, pre-built assets shipped so a clone needs no Node; four control_plane guard tests pinning no-framework/no-CDN/no-build are retired deliberately with the old dashboard.
- (assumption) Brownfield fact (sweep @baa19472): gateway runs its own asyncio loop, survives core os.execv, has DbPool, hosts no HTTP; control_plane and webhooks are core-only (role != gateway).
- (assumption) Brownfield fact: IPC unix socket accepts many connections but each accept replaces the core link; request/response frame pairs exist (ephemeral, consent, clarify); no generic query frame; ProgressEventFrame defined but unsent; HelloFrame protocol_version=1, frames extra=forbid.
- (assumption) Brownfield fact: EventBus is in-process, string topics, 22 emit sites, 10 subscribe sites; no event table; audit_log is hash-chained with REAL timestamps (breaks one-clock-one-format), only 2 chained callers.
- (assumption) Brownfield fact: aiohttp 3.13.5 in both processes, no WebSocket/SSE use in src; CommandRegistry.dispatch checks no authz; ControlPrincipal.may severities READ/WRITE/CONSEQUENTIAL used only for READ; RoutingPrompter auto-grants unknown channels via AutonomousPrompter; DurableTaskStore.enqueue is the universal ingress.
- (assumption) Brownfield fact: no TLS/cert/WireGuard code (cryptography>=42 present); STT whisper + piper TTS whole-file in run_in_executor; ChannelAdapter ABC is the channel pattern; hardware probe exists in media/image/capability.py; package data ships via hatchling Path(__file__).parent.
- (event) Seeded ARCHITECTURE-SPINE.md from template; brownfield facts and owner constraints logged.
- (decision) [ADOPTED owner 2026-09-12] Bridge web server lives in the GATEWAY process. Binds: server placement, lifecycle, IPC. Prevents: the Bridge dying on every core os.execv restart, and a second process to supervise. Core pushes events and answers live-state queries to the gateway over the existing link via new frame types.
- (decision) [ADOPTED owner 2026-09-12] The event journal is a NEW SQLite table added by migration (not audit_log, not jsonl). Binds: journal storage. Prevents: REAL-timestamp/hash-chain coupling with audit_log and log-wording coupling with jsonl. ISO text timestamps, bounded retention, resume cursor (details open).
- (question) Owner picked FULL EVENT SOURCING over the two-lane (journal as read model + one-loop write path) recommendation. Open: does he mean events become the source of truth for all platform state (platform rewrite; tension with one-copy and md-or-sqlite rules; curated md memory), or a complete journal of every state change while existing stores remain authoritative? Clarify before binding.
- (question) Owner picked WEBTRANSPORT over SSE+POST. Open: verify current iOS Safari/Chromium WebTransport support and Python HTTP/3 server options (aiohttp cannot serve it) before binding; consider fallback requirement for phones.
- (version) VERIFIED 2026-09-12 (web): WebTransport is Baseline — Safari 26.4 (March 2026) ships it on macOS and iOS without flags; caniuse global 91.3%: Chrome 97+, Edge 98+, Firefox 114+, Safari/iOS 26.4+, Chrome Android 152+, Samsung 18+. Sources: webkit.org/blog/17862, caniuse.com/webtransport, webrtc.ventures 2026/04.
- (event) CORRECTION: the recommendation text claimed WebTransport on iPhone Safari was unreliable; that was outdated training knowledge — web verification shows Baseline since Safari 26.4. Owner's WebTransport choice is viable; remaining open points are server library, fallback for pre-26.4 devices/UDP-blocked networks, and certificate trust (serverCertificateHashes confirmed for Chrome 100+, Firefox; Safari unconfirmed).
- (assumption) aiohttp cannot serve HTTP/3/QUIC; aioquic (aiortc project) supports HTTP/3 and WebTransport via H3Connection(enable_webtransport=True); its version, licence and aarch64 wheel availability to be verified before binding.
- (decision) [ADOPTED owner 2026-09-12] COMPLETE JOURNAL: every state change in every subsystem (tasks, jobs, owls, memory, consent, heals, deliveries, providers, health, channels) is emitted as a typed event into the append-only journal; existing subsystem tables remain the authoritative source of truth; the journal is a derived, append-only record (not a second copy of state). Binds: all subsystems, the Bridge read model, flight recorder, briefing. Prevents: a platform rewrite to true event sourcing, two authoritative copies of one fact, and Bridge visuals built from snapshots.
- (decision) [ADOPTED owner 2026-09-12] Live stream transport = WebTransport (HTTP/3) primary with AUTOMATIC fallback to Server-Sent Events + HTTPS POST on the same origin when the browser lacks WebTransport or UDP is blocked; both resume from the same journal cursor. Binds: gateway transport, browser client. Prevents: phones on pre-26.4 Safari or UDP-blocked networks losing the Bridge, and two divergent event protocols (one envelope, two carriers).
- (version) VERIFIED 2026-09-12 (PyPI): aioquic 1.3.0, BSD-3-Clause, requires Python >=3.10; aarch64/macOS/Windows wheel availability for 1.3.0 still to confirm before binding.
- (question) Resolved: 'full event sourcing' = complete journal (see decision). Two-lane write path (actions as tasks through consent into the one loop) not yet explicitly confirmed — asked in round 2.
- (decision) [ADOPTED owner 2026-09-12] WRITE PATH: every Bridge action (button or voice) is a typed command -> authority check by severity (READ/WRITE/CONSEQUENTIAL via the principal) -> consent through a registered 'web' prompter -> enqueued as a task in the ONE task loop (DurableTaskStore.enqueue). The Bridge never mutates a subsystem table directly. Binds: every Bridge mutation, voice commands, approvals, undo. Prevents: a back door, a second engine, two mutation paths with divergent audit.
- (decision) [ADOPTED owner 2026-09-12] CORE->GATEWAY EVENTS: core writes each event to the journal table and pushes it to the gateway over the existing IPC link as a new frame type; on core restart or link loss the gateway catches up by reading the journal from its last cursor. Binds: IPC protocol (new frame, protocol_version bump), gateway fan-out. Prevents: lost events across os.execv, polling load on small hardware.
- (decision) [ADOPTED owner 2026-09-12] FRONT END: Svelte (UI: stations, panels, Needs-you strip) + Three.js WebGPU renderer with WebGL2 fallback (Viewscreen) + TypeScript, built with Vite; pre-built assets shipped as package data and served by the gateway (Q43). Binds: all browser code. Prevents: per-station framework drift, Node on a clone. Exact versions verified before binding.
- (decision) [ADOPTED owner 2026-09-12] JOURNAL RETENTION: a setting with a 30-day default, pruned by the platform's own housekeeping (a scheduled sweep, not ad hoc). Binds: journal table, prune job. Prevents: unbounded growth on small hardware and silent loss of flight-recorder history.
- (version) VERIFIED 2026-09-12 (npm registry): svelte 5.57.0 MIT; three 0.186.0 MIT (import 'three/webgpu': WebGPURenderer falls back to WebGL2 automatically, per threejs.org manual); vite 8.3.0 MIT, engines node ^20.19.0 || >=22.12.0 (build-time only; clones receive pre-built assets); typescript 7.0.2 Apache-2.0.
- (version) VERIFIED 2026-09-12 (PyPI/GitHub): py_webauthn (webauthn) 3.0.0 BSD-3-Clause, Python >=3.10, duo-labs; wireguard-go MIT; boringtun BSD-3-Clause (userspace WireGuard).
- (constraint) VERIFIED 2026-09-12: WebTransport does not send cookies or HTTP authentication (Chromium Intent to Ship); session authentication for the WebTransport carrier must be an explicit token presented in the session handshake or first message, validated by the gateway.
- (decision) [ADOPTED owner 2026-09-12] SESSION CARRIER: a bearer token (not a cookie) authenticates every Bridge request — HTTPS pages' API calls, the SSE fallback, POST actions and the WebTransport session (presented in its first message). Binds: gateway auth, browser client. Prevents: two auth mechanisms per carrier. Follow-up open: token storage, lifetime/refresh, per-device revocation (asked round 4). Mitigation note: strict CSP with vendored-only scripts limits script-readable token theft.
- (decision) [ADOPTED owner 2026-09-12] PRIVATE NETWORK: the platform generates WireGuard keys, peer configs and a phone QR, and guides a one-time privileged setup of the host OS's own WireGuard (no embedded userspace implementation). Binds: remote-access setup flow, docs. Prevents: packaging/patching native WireGuard binaries per OS/CPU.
- (decision) [ADOPTED owner 2026-09-12] VOICE: a voice channel adapter in the gateway joins the one conversation like any channel; speech engines run in a separate supervised voice worker process (Pipecat, if spikes pass) that may run on another machine on the private network (Q27). Binds: voice placement, supervision, worker auth. Prevents: speech load stalling the Bridge/gateway, calls dropping on core restarts.
- (decision) [ADOPTED owner 2026-09-12] JOURNAL CONTENT: events carry metadata only — type, actor, target, outcome, timings, ids, attention class — never message text, memory content, prompts or secrets; opening a record fetches content from the owning table through the normal authority checks. Binds: event schema, every emitter, a leak guard. Prevents: a second copy of private content, secrets persisted in telemetry.
- (decision) [ADOPTED owner 2026-09-12] DEVICE TOKENS: each signed-in device holds its own bearer token in browser IndexedDB, valid ~30 days of inactivity, rotated on use, listed in the Security station and revocable per device; passkey sign-in only for a new device or after revoke/expiry. Binds: session store (sqlite, owner-scoped), Security station, gateway auth. Prevents: re-prompting on every phone reload, and an unrevocable shared token.
- (decision) [ADOPTED owner 2026-09-12] RECORDING: one journal API — subsystems call journal.record(TypedEvent) at the exact site the action happens; every event type lives in one versioned registry; a tripwire test fails any state-mutation path that records nothing. The in-process EventBus is not the journal's source. Binds: every subsystem, journal/. Prevents: silent coverage gaps and stringly-typed drift.
- (decision) [ADOPTED owner 2026-09-12, placement vote] PACKAGES: src/stackowl/journal/ (event types registry, store, recorder, retention — imported by every subsystem, imports nothing Bridge-specific); src/stackowl/bridge/ (gateway-hosted web server, transports WebTransport+SSE, sessions, API, static assets); src/stackowl/voice/ (voice worker + gateway adapter). control_plane/ deleted when the Bridge ships (Q45). Binds: dependency direction journal <- subsystems, bridge -> journal, voice -> channels contract. Prevents: subsystems importing from bridge, and the old dashboard's guards leaking into the new server.
- (decision) [ADOPTED owner 2026-09-12] BUILT ASSETS: front-end source lives in the repo (web/bridge/); built minified assets are committed into src/stackowl/bridge/static/; a test fails when source and committed build drift. Binds: build pipeline, repo layout, packaging. Prevents: a git clone without a front end, and network or Node at install.
- (decision) [ADOPTED owner 2026-09-12] CERTIFICATES: the platform creates its own certificate authority constrained by X.509 name constraints to the install's host names and home/WireGuard IP ranges; CA key in the platform secret store; short-lived server certificates auto-renewed and used for both HTTPS (TCP) and HTTP/3 (UDP). Provisional until spike B1 proves stock iPhone/Android trust. Binds: bridge TLS, setup guide. Prevents: a leaked CA key forging certificates for arbitrary sites, and certificate expiry outages.
- (decision) [ADOPTED owner 2026-09-12] CORE QUERIES: the gateway obtains core-only live state (pending consent prompts, running workers, breakers, context windows) via new typed request/response frames on the existing gateway<->core link with a protocol version bump; no second socket. Binds: ipc/frames, core handlers, bridge API. Prevents: a second connector displacing the core link, and a second reconnect path.
- (decision) [ADOPTED owner 2026-09-12] ATTENTION: one attention policy module in journal/ classifies every event as ambient, needs-you or fault from its type, outcome and healed status; emitters and the browser never classify. The Needs-you strip, accent colour, sounds, briefing and phone notifications all read that one classification. Binds: journal/, bridge/, notifications. Prevents: amber drifting between subsystems and a second copy of the rules in the browser.
- (decision) [ADOPTED owner 2026-09-12] PHONE ALERTS: needs-you items notify through the owner's existing Telegram channel AND standard Web Push (VAPID, end-to-end encrypted payload) for the installed Bridge app; Web Push relays through browser-vendor push services by design. Binds: notifications, bridge service worker. Prevents: a single delivery path failing silently. NOTE: this is a second named exception to principle 7 (self-hosted) alongside the Q46 DNS/ACME fallback; full-picture.md §2 and §12 must be amended to say so.
- (decision) CONVENTION (architect, within adopted decisions): journal event envelope = {event_id (UUIDv7 text), cursor (journal table INTEGER PRIMARY KEY, monotonic, the resume point for both carriers), type (registry name, dotted, e.g. task.claimed), schema_version, occurred_at (ISO-8601 UTC text, one clock one format), actor_kind+actor_id, target_kind+target_id, outcome (ok|failed|healed|parked|pending), attention (set only by the attention policy), trace_id, duration_ms?}. Metadata only. Binds: journal/, both carriers, the browser. Prevents: two envelope shapes for WebTransport vs SSE, and ad-hoc timestamps.
- (decision) CONVENTION: liveness — the gateway emits a heartbeat on every open carrier at a fixed interval; the browser shows the stale state (breathing stops, last-event age shown) when a heartbeat is missed; heartbeats are not journal rows. Binds: bridge transports, front end. Prevents: a dead stream that still looks alive (truthful motion).
- (decision) CONVENTION: one gateway port serves HTTPS (TCP) and HTTP/3 (UDP) with the same certificate; bind follows the existing control-plane bind setting (every interface by default, ESC-172), protected by TLS plus passkey-issued device tokens; the SSE fallback and POST actions share the HTTPS origin. Binds: bridge server, setup guide. Prevents: split origins breaking token scope and fallback.
- (decision) CONVENTION: identity — one owner record (sqlite) with passkeys (py_webauthn), device sessions and a one-time recovery code; the first passkey is enrolled with the Q29/L1 setup code; every web session maps to the default principal plus a web:<device> alias in the existing identity map; new devices approved only from a signed-in device or the recovery code (Q48). Binds: bridge/sessions, tenancy identity. Prevents: a second identity model beside the cross-channel alias map.
- (decision) CONVENTION: consent — a real 'web' prompter registers in the gateway and 'web' joins the core's prompter channel set so consent for the web channel is never auto-granted; mirrored approvals (Telegram buttons + Needs-you strip) resolve first-answer-wins with the losing surface updated; device approvals are never mirrored to Telegram (Q48). Binds: tools/consent routing, bridge API. Prevents: AutonomousPrompter granting web actions, double-approval.
- (decision) CONVENTION: voice worker is supervised by the gateway like the core (restart on crash); a remote worker on the private network authenticates with its own revocable worker token; browser audio uses WebRTC to the worker (Pipecat transport) — transport and tiering provisional until spikes S1/S5/S9 report. Binds: voice/, runtime supervision. Prevents: an unsupervised speech process and an unauthenticated remote worker.
- (assumption) Provisional until spikes report (full-picture §9): B1 phone trust of the name-constrained CA over LAN/WireGuard; B2 carrier behaviour and resume at real event rates on mobile data; B3 Viewscreen battery/thermal on mid-range phones (tier thresholds); B4 event-type to visual grammar; S1/S5/S9 Pipecat, transport and interruption semantics; S3/S4 STT/TTS per tier.
- (question) CONFLICTING EVIDENCE 2026-09-12 on the adopted name-constrained CA: caniuse issue #6969 and Apple-related search results quote a 2016 source that iOS/macOS do not support nameConstraints and REJECT any chain containing the (critical) extension; a 2025/26 article says enforcement is broadly present but must be tested per client. No current Apple documentation found. If Apple still rejects, the owner's iPhone cannot trust the constrained CA. Do not bind until verified (BetterTLS / spike B1); re-ask the owner for the fallback order.
- (decision) [ADOPTED owner 2026-09-12] CERTIFICATE FALLBACK ORDER: (1) name-constrained per-install CA; (2) if spike B1 shows a stock iPhone rejects or ignores name constraints, an UNRESTRICTED per-install CA whose private key is generated on the host and never leaves it (secret store), issuing only short-lived Bridge server certificates, with an explicit install-time warning about what trusting it means; (3) the Q42/Q46 free-domain + public certificate fallback only if the private-CA route proves unworkable. Binds: bridge TLS, setup guide, spike B1 pass criteria. Prevents: an iPhone that cannot reach the Bridge, and a CA key ever leaving the host.
- (event) Coaching frontier empty (rounds 1-5 + CA fallback). Moving to Finalize: distill in a subagent, reconcile inputs, reviewer gate.
- (version) VERIFIED 2026-09-13 (PyPI): aioquic 1.3.0 uploaded 2025-10-11, requires Python >=3.10, abi3 wheels for manylinux_2_28 aarch64 and x86_64, musllinux aarch64/x86_64, macosx_11_0 arm64 and 10_9 x86_64, win32/win_amd64/win_arm64, plus sdist — the 'wheels to confirm' open question is closed.
- (decision) [ADOPTED owner 2026-09-13] ATOMIC RECORDING: a journal event is written in the SAME SQLite transaction as the state change it records (transactional outbox); the gateway push happens only after commit. State outside SQLite (md memory files) records immediately after its write, and a failed record reports the journal contributor degraded. Binds: journal.record API (takes the caller's transaction/connection), every emitter. Prevents: the Bridge showing an event for a rolled-back change or missing a committed one.
- (decision) [ADOPTED owner 2026-09-13, placement vote] The Bridge's conversation channel adapter (channel name 'web') lives in src/stackowl/bridge/ and implements the existing channel contract. Binds: bridge/, channel registry. Prevents: channels/ importing bridge transports (keeps 'nothing imports bridge').
- (decision) [ADOPTED owner 2026-09-13, placement vote] The authority model (READ/WRITE/CONSEQUENTIAL severities and the principal 'may' check) moves from control_plane/ into the existing src/stackowl/authz/ package before control_plane is deleted; every surface (web, voice, future) asks authz. Binds: authz/, bridge/, voice/. Prevents: surfaces importing bridge for authority or copying it.
- (question) Reconcile (mockup briefs) found 9 gaps: (1) envelope needs a typed metadata-only attrs block (memory kind, tool name, channel, job name, fuel); (2) closed actor/target kind list and a dead_lettered outcome; (3) one startup snapshot query returning its cursor, front end never hardcodes rings/lists; (4) one narrator in journal/ turning events into plain-English sentences at delivery time, shared by Ship's log, briefing, Telegram, Web Push; (5) record_ref (owning table + row id) plus one authorised record query; (6) needs-you lifecycle: resolves links, open set definition, fault vs amber clash with the brief; (7) design tokens + logo module in web/bridge with drift guard sourced from logo/ (ICON_SVG lives in control_plane/page.py which is deleted), fonts are SIL OFL which principle 6 does not list — owner policy call; (8) one client store owning stream/cursor/heartbeat/stale/power per browser; (9) captions travel on the web/voice channel not the journal; presence derived in one place.
- (question) Reconcile (research 01+03) 13 gaps: AD-20 battery-saver trigger unbuildable (iOS Low Power Mode undetectable) -> tier from measured frame time + reduced-motion, render-on-demand convention; AD-11 SSE over HTTP/1.1 hits ~6 connections/origin -> one stream per browser shared across tabs; barge-in cancel must route through AD-1; UI mover = real record (projected due jobs have no event); voice consent missing (voice channel would auto-grant); one server-side voice state machine; browser->worker auth breaks one origin/device token -> WebRTC signalling via gateway; voice fallback to push-to-talk; one STT/TTS seam via media/stt|tts selectors; licence rule must cover runtime-downloaded weights, deny-list test, remove auto-installed Piper, ban Web Speech API; iOS audio gesture/background rules; spikes as a gate table incl S2 S6 S7; missed heartbeat or visibility change forces reconnect from cursor.
- (question) Reconcile (full picture) 13 gaps: one voice state machine AD (Q24/Q39/Q40/Q47/Q50 are decided rules, not spike values); reversibility/undo gate AD (§8.12, Q11/Q35/Q36/Q49/Q52; voice commands carry trace_id); durable Needs-you item model (persisted until resolved, priority/intensity, kind, id for Q38 deep links and first-answer-wins; define or drop fault vs Q34); sound Convention (Q51/Q39, echo-cancelled output); AD-2 must list tool calls, model calls, delegation hops and state the fate of ProgressEventFrame/EventBus/TUI progress; every event target is a durable row (incidents, finished tasks, per-turn decisions need owning tables); severity check in the ONE shared command dispatch; channels without a registered prompter fail closed; ban browser cloud speech and third-party STUN/TURN; Q44 opt-in NVIDIA download with licence shown; setup code lives in control_plane which AD-7 deletes — relocate (placement vote) and carry L3 rule; B2 re-scope (WebTransport chosen), S6 S7 unmapped, fail branches; CA rotation/revocation deferred; decide spine OQ1-3 before epics.
- (decision) [ADOPTED owner 2026-09-13] FONT LICENCE: SIL Open Font License is allowed for fonts (vendored into the Bridge); principle 6 becomes MIT/Apache/BSD, CC-BY for weights, SIL OFL for fonts. Binds: web/bridge assets, licence guard. Prevents: replacing the approved typefaces or silently violating the licence rule.
- (decision) [ADOPTED owner 2026-09-13, placement vote] OWNER IDENTITY lives in src/stackowl/authz/identity/: setup code (relocated from control_plane), owner record, passkeys, device sessions, recovery code — alongside the authority model. Binds: authz/, bridge sessions. Prevents: identity scattered across bridge/ and tenancy/, and loss of the Q29 setup code when control_plane is deleted.
- (decision) ARCHITECT resolutions from reconcile (within adopted decisions): typed COMMAND task kind with a deterministic handler registry on the one loop; one action-policy gate (reversible->run+undo, crew request->read-back, irreversible->read-back+tap, not pre-authorised->Needs-you; voice commands carry trace_id); durable Needs-you items table (kind, intensity, record_ref, opened/resolved cursors, first-answer-wins) and attention = ambient|needs_you with intensity normal|high (drop 'fault', Q34); envelope attrs block typed per registry + closed actor/target kinds + dead_lettered outcome + record_ref; every openable target is a durable row; one snapshot query with its cursor; one narrator in journal/; one client store per browser (stream shared across tabs, forced resume on missed heartbeat/visibility, heartbeat interval in server hello); rendering tier from measured frame time + reduced-motion, render-on-demand; one server-side voice state machine, captions/presence off-journal, WebRTC signalling via gateway with device token, media host-candidates only (no third-party STUN/TURN), worker never connects to core.sock, push-to-talk fallback, engines via media/stt|tts, iOS gesture/background rules, voice consent prompter; prompter registration derived from config and unknown channels fail closed, reply_target on ConsentRequestFrame; severity check in the one CommandRegistry dispatch; Hello protocol_version checked, mismatch refuses the link and the gateway self-restarts under supervision, unknown frames WARN, unwired progress/steer/stop/running frames deleted and replaced; journal cursor INTEGER PRIMARY KEY AUTOINCREMENT, gateway writes its own events via its DbPool, fan-out merges by cursor; gateway DbPool for catch-up/snapshot/record/enqueue; web:* principals resolve to the owner in code (no YAML aliases); relocate severities and setup code before control_plane deletion; licence rule covers runtime-downloaded weights with a deny-list tripwire, Piper auto-install removed, browser cloud speech banned, Q44 opt-in NVIDIA download; sound convention (Q51/Q39, echo-cancelled output); health contributors for recorder and bridge server, idempotent prune seeder; migrations only (runtime DDL is not precedent), numbers allocated at merge; voice worker supervised with the gateway per-loop Supervisor pattern; tripwires via scripts/tripwires.sh; PLACEMENT: docstrings introduced as a new convention; shared capability probe; spike gate table with fail branches (B2 re-scoped to WebTransport/SSE resume at real rates; S2, S6, S7 added); Deferred: CA rotation/revocation/per-device trust, NAT traversal.
- (decision) [ARCHITECT, within owner logo decision 2026-09-12] LOGO SOURCE: logo/stackowl-mark.svg and logo/stackowl-logo.svg (committed 370ce44c) are the single source of the official owl mark once control_plane/page.py (ICON_SVG) is deleted; web/bridge imports the mark from logo/, the built app icon and inline mark are generated from it, and a drift tripwire asserts every shipped copy draws the same path. Binds: web/bridge, bridge/static, logo/. Prevents: the mark being lost with control_plane or redrawn by an epic (the mark is never redesigned).
- (event) Reviewer gate: rubric (reviews/review-rubric.md) verdict not safe for epics — C1 no canonical host name (passkeys cannot bind to IPs; LAN vs WireGuard origins split tokens/push/PWA), C2 no router-change-free WireGuard reachability, highs H1-H9 (consent runs in core; dispatch needs PipelineState; 5s tick; approval items vs consent waiters; three identity concepts; md/Kuzu records unopenable; flight recorder base state; carrier framing for chat/captions/results; login brute-force brake dropped; backup/restore of CA/identity/VAPID/WireGuard keys; performance budgets/slow clients).
- (event) Reviewer gate: adversary (reviews/review-adversary.md) — C1 three mutation paths (command tasks, inline slash commands, LLM tools calling mutators directly e.g. cronjob.py, owl_build.py) -> one command declaration per action, tools submit commands; C2 first-answer-wins not atomic and items outlive consent waiters -> one conditional resolver, items expire with waiter; C3 core-only restart runs migrations under old gateway -> compare migration number + registry hash; highs: consent in two processes, record_ref to rows deleted after 1 day, no event version evolution rule, fan-out on pruned cursors, no notifier owner, per-process registries, setup-code damage re-enables Telegram enrolment, web sessions as separate identity, attention set before heal.
- (event) Reviewer gate: versions (reviews/review-versions.md) — C1 Chrome rejects WebTransport certs chained to a user-installed private CA (ERR_QUIC_CERT_ROOT_NOT_KNOWN) -> serverCertificateHashes (short-lived self-signed ECDSA, Chrome/Firefox/Safari 26.4); H1 aioquic no release/commit since Oct 2025, WebTransport draft-02, Safari 26.4 draft compatibility unconfirmed; H2 EventSource cannot send Authorization header -> SSE via fetch stream; H3 svelte-check supports TS 5/6 only, TS 7.0.2 needs experimental flags; H4 passkeys cannot use IP RP IDs; H5 WireGuard has no NAT traversal.
- (version) VERIFIED 2026-09-13 (web): WebAuthn RP ID cannot be an IP address or public suffix and requires a secure context (web.dev RP ID deep dive; w3c/webauthn #1358); .local support unconfirmed. Behind carrier-grade NAT, UPnP/NAT-PMP/PCP on the home router do not help (the NAT is upstream); options are public IPv6, port forward where no CGNAT, or a self-hosted relay (own VPS WireGuard hub / mesh relay).
- (question) OWNER DECISIONS REOPENED by evidence: (A) canonical Bridge host name — own CA path now has three compounding problems (Apple name constraints unverified, Chrome QUIC rejects private roots, passkeys need a domain anyway) vs promoting the Q42/Q46 public-domain + public certificate route; (B) remote reachability behind CGNAT — Q26 'no router change required' is not achievable everywhere: probe ladder IPv6 / port mapping / guided own-VPS relay / honest LAN-only. Ask after the security review.
- (event) Reviewer gate: security (reviews/review-security.md) — C1 on-screen tap is a bearer POST (holder of a device token approves irreversible); C2 no CSP/Trusted Types, script-readable 30-day token; C3 voice transcripts treated as owner orders, voice gets owl-authority grants unprompted; C4 CA key readable by the agent's unattended shell, fallback step 2 keeps an unrestricted signing key online, revocation deferred; highs H1-H10 (consent composition, agent-writable requester/standing-authority/session rows, SSE token in URLs and replay/duplicate commands, token reuse/lifetime/revocation deadline, device-approval phishing, hijacked Telegram claims install, recovery-code strength and IPv6 rate-limit bypass, schema-only journal leak check, unpinned runtime downloads, CA/WireGuard trust-on-first-use).
- (decision) [ADOPTED owner 2026-09-13] NAME + CERTIFICATES (owner kept own CA over promoting the public-domain route despite evidence): a private per-install CA with a LOCAL host name; CA trusted on each owner device; WebTransport uses short-lived self-signed certificates via serverCertificateHashes (Chrome rejects private roots for QUIC); passkey support on a private name on iPhone is unverified — spike B1 is the gate. Architect mitigation for the agent-readable key: the CA key is used at setup to sign the Bridge server certificate and then destroyed (renewal = new CA + guided device re-trust); no CA signing key is kept online.
- (decision) [ADOPTED owner 2026-09-13] REACHABILITY: IPv4 on the home network only (owner answer 'Ipv4 only and for home network'). Reverses Q9 'reachable from anywhere' and removes the need for built-in WireGuard; scope of removal being confirmed with the owner.
- (decision) [ADOPTED owner 2026-09-13] STEP-UP: a tap is enough — irreversible actions, consequential approvals, owl-authority grants and device approvals keep Q16's read-back plus on-screen tap (no passkey re-prompt). Architect note: device-bound tokens (below) mean the tap must come from the signed-in device's own key.
- (decision) [ADOPTED owner 2026-09-13] DEVICE-BOUND TOKENS: at sign-in the browser creates a non-extractable WebCrypto key; every request and stream is signed with it (proof of possession), so a copied token is useless elsewhere; strict CSP with Trusted Types; hard lifetime cap; reuse detection revokes the device. Binds: authz/identity, bridge auth on every carrier, web client.
- (decision) [ADOPTED owner 2026-09-13] HOME NETWORK ONLY, WIREGUARD REMOVED: the Bridge is reachable only on the home network over IPv4; built-in WireGuard, remote access, CGNAT/relay work and the Q42/Q46 public-domain fallback are removed from the architecture and the build (retired means deleted, no seat for later); away from home the owner uses Telegram. AD-15 is retired (ID not reused). Supersedes Q9 'reachable from anywhere' and Q26.
- (decision) [ADOPTED owner 2026-09-13] ALERTS AWAY FROM HOME: keep BOTH Telegram and Web Push. Tapping a Web Push notification when the Bridge is unreachable (away from home) shows a cached metadata-only summary of the item from the service worker, with 'open at home' and a link to continue in Telegram; at home it opens the item in the Bridge. The service-worker cache holds metadata only (never content). Binds: bridge service worker, notifier, AD-19. Prevents: a notification that dead-ends off the home network, and content cached on the phone.
- (decision) [ADOPTED owner 2026-09-13] PROCESS ISOLATION: gateway and core run as the SAME OS user so every clone works out of the box on any hardware. Residual risk recorded: the agent's own tools (e.g. shell) can read files the gateway stores (device-token signing material, Web Push VAPID keys, identity database). Mitigations that stand: the CA key is destroyed after signing (AD-14); authz identity, requester-kind, standing-authority and session rows are writable only through authz APIs never exposed as agent tools (tripwire); device tokens are device-bound so gateway-side material alone cannot impersonate a device (AD-16). A separate gateway OS user is not required.
- (event) spine finalized 2026-09-13: 41 AD ids (40 live, AD-15 retired), lint 0 findings, 4 mermaid diagrams parse with mermaid 11; reconcile (4 inputs) and reviewer gate (rubric, versions, adversary, security) applied; owner decisions A1-A6 mirrored into full-picture.md (1772783a).
