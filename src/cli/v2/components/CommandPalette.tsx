/** Cmd+K-style palette for /skills /mcp /sessions /owls /help. Phase 3. */

import { Box, Text } from "ink";
export function CommandPalette({ onClose: _onClose }: { onClose: () => void }) {
  return (
    <Box borderStyle="round">
      <Text>Command Palette — Phase 3</Text>
    </Box>
  );
}
