/* ConversationView styles — design tokens only, no literal colors. */
ConversationView {
    layer: base;
    background: $color-bg;
    border: solid $color-border;
    height: 1fr;
}
ConversationView VerticalScroll {
    background: $color-bg;
    color: $color-text-primary;
}
ConversationView VerticalScroll:focus {
    border: solid $color-accent;
}
